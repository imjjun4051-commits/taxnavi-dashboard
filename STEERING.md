# 프로젝트 Steering: 택스네비 — 중소기업 재무분석 및 절세전략 웹서비스

> 이 파일은 한 번 작성하고 계속 참조합니다. Claude Code는 `CLAUDE.md`를 자동으로 읽고, `CLAUDE.md`가 이 파일을 가리킵니다.
> (2026-09-03 개정: 색상·서체·API·데이터·세무 규칙을 검토 결과에 맞게 수정 — 상세는 DEVELOPMENT_LOG.md)

## 1. 프로젝트 정체성
- **서비스명**: 택스네비
- **한 줄 소개**: 내년 실적을 미리 보고, 세금은 미리 설계하세요 — 광주·전남 중소기업 재무 내비게이터
- **목표**: 광주/전남 중소기업이 미래 실적을 예측하고 절세 의사결정을 하도록 지원
- **사용자**: 재무팀이 없는 중소기업 대표 (비전공자)
- **핵심 가치**: 사후 정산이 아닌 **사전 설계** 방식의 세금 관리

## 2. 기술 스택 (고정)
- **프론트엔드**: HTML5 + CSS3 + Vanilla JavaScript (단일 `index.html`)
- **데이터 저장**: `window.localStorage` (서버 없음)
- **외부 라이브러리**: 금지 (차트·탭·다이얼로그 모두 직접 구현)
  - **유일한 예외**: 서체 Pretendard (jsDelivr CDN `<link>`). 오프라인이면 시스템 산세리프로 자동 대체되며 기능에는 영향 없음
- **외부 API**: 3개, 모두 서버 프록시(Vercel 함수)를 거쳐 호출 — 로컬은 `vercel dev`, 배포는 `vercel --prod`. 더블클릭(file://)은 서버가 없어 해당 기능이 샘플/오류 안내로 대체됨
  1. **기업마당**(bizinfo.go.kr) 지원사업정보 Open API — `api/bizinfo.js`, 인증키는 서버(Vercel 환경변수 `BIZINFO_API_KEY`)에 등록. 화면에 인증키 입력란·"불러오기" 버튼 없음(2026-09-03 삭제). 지원사업 화면 진입 시 자동으로 실시간 공고를 불러오고, 실패하면 자동으로 샘플로 대체하며 그 사실을 소스 칩에 표시. EC2 대신 Vercel 함수로 CORS 해결(2026-09-03)
  2. **DART**(전자공시시스템) — 업종 평균(코스닥·코넥스 상장기업 실제 재무제표) 계산용. `tools/update-industry-avg.js`가 로컬에서 직접 호출(런타임 프록시 아님, `.env.local`의 `DART_API_KEY` 사용), 결과를 `docs/industry-avg.json`에 저장 후 `node tools/inline-data.js`로 동기화. 주기적 캐싱 방식(수동 재실행)
  3. **OpenAI GPT** — ERP CSV 재무데이터 자동 인식용. `api/parse-financials.js`, 인증키는 서버(Vercel 환경변수 `OPENAI_API_KEY`, **2026-09-03 기준 미등록**) 사용. CSV 원문을 GPT에 보내 매출액·영업이익·인건비·유형자산·감가상각비·이자비용을 Structured Outputs로 추출, 재무데이터 표에 자동 채움(단위 자동 환산)
- **데이터 파일 구조**
  - 원본: `sample-data.json`, `docs/tax-rules.json`, `docs/industry-avg.json`, `docs/government-support.json`
  - 실제 로딩: `index.html` 안의 `<script type="application/json" id="data-...">` 인라인 블록
    (`file://`로 열면 브라우저가 `fetch('docs/*.json')`을 차단하므로 인라인 필수)
  - **규칙: JSON 원본을 고치면 `node tools/inline-data.js`로 index.html을 함께 갱신한다**

## 3. 코드 품질 기준 (필수)
- **비전공자 이해도**: 함수·변수명은 한국어 또는 쉬운 영어, 모든 함수 위에 한국어 주석
- **오류 없음**: F12 콘솔 JavaScript 오류 0개 (폰트 CDN·API 차단 같은 네트워크 실패는 오류로 보지 않음)
- **데이터 유지**: 입력 즉시 자동 저장(300ms 디바운스) → 새로고침해도 유지
- **반응형**: 모바일 320px ~ 데스크톱 1920px, 가로 스크롤 없음 (재무 표는 표 안에서만 좌우 스크롤)
- **입력 검증**: 숫자만 허용, 필수 항목은 0 이상. 단 **영업이익은 음수(적자) 허용**
- **접근성**: 탭 `role=tablist/tab/tabpanel`, 입력 `label`/`aria-label`, 포커스 링 표시, 작은 글씨 대비 4.5:1 이상

## 4. UI/UX 원칙
- **화면 구성** (2026-09-03 대시보드 리디자인): 좌측 다크 사이드바(≥1024px 240px / 768~1023px 아이콘 72px) + 라이트 본문 + 모바일(<768px) 하단 탭바. 화면 5개: 개요 · 입력 · 분석결과 · 절세전략 · 지원사업. 툴바(제목·회사/예측연도 칩·자동 저장). 자유 이동 가능하되 개요 화면 빈 상태에 "입력 시작" 안내. (완료 상태를 점으로 표시하던 기능은 2026-09-03 삭제)
- **한국어**: 모든 문구·에러 메시지·숫자 포맷(천단위 쉼표). 금액은 **입력 백만원 / 표시 원 + 억·만원 요약**
- **디자인 토큰** (오렌지 시그니처 + 쿨톤 그레이, hex 값 사용)
  | 용도 | 토큰 | 값 |
  |---|---|---|
  | 1차 CTA 버튼·배지·그래프 | `--primary-500` | `#FF6000` |
  | 본문 속 강조 글씨·링크 (작은 글씨 대비 확보) | `--primary-600` | `#CC4D00` |
  | 연한 오렌지 배경(칩·요약 카드) | `--primary-50` | `#FFEFE5` |
  | 본문 텍스트 | `--gray-800` | `#333D4B` |
  | 보조 텍스트 | `--gray-600` | `#6B7583` |
  | 페이지 배경 / 카드 배경 | `--gray-100` / `#FFFFFF` | `#F3F4F6` |
  | 테두리 기본 / 약한 구분선 | `--gray-300` / `--gray-200` | `#D1D6DC` / `#E5E7EA` |
  | 차트: 예측 / 실적·평균 기준 | `--primary-500` / `--gray-600` | 오렌지 / 중립 회색 |
  | 사이드바 배경/글자/활성 | `--sidebar-bg` / `--sidebar-fg` / `--sidebar-active` | gray-900 / gray-300 / primary-500 (기존 값 매핑, 새 hex 없음) |
- **서체**: Pretendard (Variable), 대체 -apple-system, Apple SD Gothic Neo, Malgun Gothic
- **크기 스케일**: 제목 23px(700) · 카드 제목 17px(600) · 본문 15px · 보조 13px · 캡션 11~12px
- **라운드 코너**: 입력칸 8px · 버튼 12px · 카드 16px · 칩 999px (날카로운 모서리나 순수 필 하나로 수렴하지 않기)
- **간격**: 4px 배수 (4/8/12/16/20/24/32/40/48)
- **차용 범위**: 참고 디자인 시스템에서 **색·서체·라운드 코너라는 시각 언어만** 빌린다. 해당 브랜드의 로고·이름·워드마크·패키지명은 화면·코드·문서 어디에도 쓰지 않는다
- **다이얼로그**: `window.confirm/alert` 대신 자체 다이얼로그·토스트 사용

## 5. 기존 기능 보호 원칙
- 새 기능 추가 시 기존 기능 삭제 금지
- 새 기능 추가 후 "기존 기능 모두 동작 확인" (8항 검증 절차)
- 수정 후 **"어떤 부분이 수정되었는지" 요약 필수** — 전체 파일 출력은 금지 (토큰 낭비)

## 6. 데이터 관리
- **저장 위치**: `window.localStorage`, JSON 문자열
- **저장 키 3개**
  | 키 | 내용 |
  |---|---|
  | `재무제표` | 기업정보 · 재무데이터(3개년) · 내년계획 · 저장시각 |
  | `절세전략` | 마지막 계산 결과(전략별 적용 여부·절감액) · 계산시각 |
  | `지원사업_검색` | 지역/분야/정렬/마감숨김 필터 · API 캐시(인증키는 서버에만 있어 저장하지 않음) |
- **초기화**: "데이터 초기화" 버튼 → 자체 확인 다이얼로그 → 위 3개 키 모두 삭제 + 화면 초기화
- **저장 시점**: 입력칸 값이 바뀌는 즉시(디바운스 300ms) + "분석 시작" 클릭 시

## 7. 세무 규칙 관리 원칙
- 모든 세율·공제 수치는 `docs/tax-rules.json`에 **기준연도 · 확인일 · 법령 근거 · 출처 URL**과 함께 둔다. 코드에 숫자를 직접 쓰지 않는다
- 2026년 기준 (2026-09-03 확인)
  - **법인세**: 법인세법 제55조 — 과세표준 2억 이하 10% / 200억 이하 20% / 3,000억 이하 22% / 초과 25% (2026.1.1. 이후 개시 사업연도) + **지방소득세 = 법인세의 10%**. 과세표준은 영업이익으로 단순 추정하고 "추정치"로 표기
  - **부가세**: 예상 납부세액 = (과세 매출 − 과세 매입) × 10%. 매입액 미입력 시 매출세액만 표시하고 그 사실을 표기
  - **통합고용세액공제**(조특법 §29의8): 증가 인원 1인당 정액, 3년 (수도권 외 중소기업 청년등 1,000/1,900/2,000만원, 그 외 700/1,200/1,300만원)
  - **통합투자세액공제**(조특법 §24): 당해 신규 설비투자액 × 10%(중소기업 일반) + 3년 평균 초과분 × 10%(기본공제의 2배 한도). 유형자산 잔액이 아닌 **신규 투자액** 기준
  - **근로소득증대세제**(조특법 §29의4): 직전 평균 임금증가율 초과 임금증가분 × 20%. ("인정상여금 세제혜택"이라는 이름은 오류 — 인정상여는 불이익 개념)
  - **신고·납부기한 연장**(국세기본법 §5): 기한이 토요일·일요일·공휴일이면 다음 평일로 연장 — 개요 화면 "다가오는 세금 일정"에 반영(`docs/tax-rules.json`의 `공휴일.목록`, 현재 2025~2027년만 등록·매년 갱신 필요). 분석결과 화면의 분기표 문구("4월 25일" 등)는 특정 연도에 매이지 않는 일반 설명이라 연장 반영 대상 아님
- 화면 하단과 절세 탭에 **면책 문구**("단순 추정치, 신고 전 세무사 확인") 항상 표시
- 시나리오 가정: **인건비·감가상각비 고정, 그 외 비용은 매출과 같은 비율로 변동**. 분기는 연간 ÷ 4 균등 (계절성 미반영 표기)

## 8. 검증 절차 (변경 후 매번)
1. `index.html` 더블클릭 → F12 콘솔 JavaScript 오류 0개
2. 샘플 데이터 불러오기 → 분석 시작 → 카드·차트·분기표·업종비교 표시
3. 새로고침 → 입력값·탭 완료 표시 유지
4. 320px(모바일) / 768px / 1920px에서 가로 스크롤 없음
5. 데이터 초기화 → LocalStorage 비어 있음
6. (선택) `python3 tools/test-e2e.py` (file://) 또는 `TEST_URL=http://localhost:3000 python3 tools/test-e2e.py` (vercel dev, 프록시 포함) — 위 항목을 자동으로 검사
