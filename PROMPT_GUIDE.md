# 📋 Claude Code 최적화 프롬프트 작성 규칙 — 텍스내비 (2026-09-03 개정판)

## ✅ 프롬프트 최적화 전략
1. **간결하고 명확하게**
   - ❌ "안녕하세요 Claude! 저는 현재 웹 개발 프로젝트를… (장황함)"
   - ✅ "현재 상태: index.html에 입력폼 있음. 추가할 것: 재무분석 결과 카드"
2. **재활용 가능한 템플릿 만들기**
   - 프로젝트 규칙은 `STEERING.md`, 요구사항은 `SPECS.md`에 한 번만 쓰고, 이후 프롬프트에서는 "참조"만 한다
   - **`CLAUDE.md`는 Claude Code가 자동으로 읽는 파일**이다. 여기에 "STEERING.md·SPECS.md를 먼저 읽을 것"을 적어 두면 매 프롬프트에 규칙을 반복하지 않아도 된다
3. **코드 설명을 함께 요청**
   - 코드 작성만 시키지 말고 "주요 함수 3개를 비전공자에게 설명"처럼 이해를 위한 설명을 요청

## 🗂️ 프로젝트 폴더 구조
```
텍스내비/
├─ CLAUDE.md                    ← Claude Code 자동 로드 규칙 (STEERING/SPECS를 가리킴)
├─ README.md                    ← 프로젝트 개요 · 실행 방법
├─ STEERING.md                  ← 프로젝트 규칙 (한 번만 작성, 계속 참조)
├─ SPECS.md                     ← 요구사항 + 설계 + 작업목록 + 테스트 체크리스트
├─ PROMPT_GUIDE.md              ← 이 문서 (프롬프트 규칙 + 템플릿)
├─ DEVELOPMENT_LOG.md           ← 진행 기록 (매 요청 후 갱신)
├─ index.html                   ← 메인 웹앱 (데이터 인라인 포함)
├─ sample-data.json             ← 테스트 데이터 (원본)
├─ docs/
│  ├─ tax-rules.json            ← 절세 규칙 데이터 (기준연도·출처 포함)
│  ├─ government-support.json   ← 정부지원사업 목록 (샘플)
│  └─ industry-avg.json         ← 업종별 평균 통계 (샘플)
└─ tools/                       ← 개발 보조 (선택)
   ├─ inline-data.js            ← JSON 원본 → index.html 인라인 블록 동기화
   └─ test-e2e.py               ← Playwright 자동 검증 (49개 검사)
```

## 📝 1단계: STEERING.md — 작성 완료 (파일 참조)
## 📊 2단계: SPECS.md — 작성 완료 (파일 참조)

## 💻 3단계: 초기 구현 프롬프트 (템플릿)
> 텍스내비 1차는 이미 구현되어 있습니다. 새 프로젝트에서 같은 방식으로 시작할 때 쓰는 템플릿입니다.

```
당신은 지금부터 "텍스내비(중소기업 재무분석 및 절세전략 웹서비스)" 프로젝트의 개발 담당 AI입니다.

## 참고 자료
- 프로젝트 규칙: STEERING.md
- 요구사항 및 설계: SPECS.md
- 샘플 데이터: sample-data.json
- 세무 규칙: docs/tax-rules.json (숫자를 코드에 직접 쓰지 말고 이 파일을 읽어 쓸 것)

## 1차 구현: 입력폼 + 기본 계산 (SPECS.md Task 1~8)
1. HTML 폼: 기업정보(업종·소재지·상시근로자 수·기준연도) + 재무데이터 3개년 표(백만원) + 내년 계획
2. CSS: STEERING.md 4항 디자인 토큰(오렌지 #FF6000 / #CC4D00, 쿨그레이) 기반 카드형 반응형 레이아웃
3. JavaScript
   - 입력값 검증: 숫자만, 필수 항목 0 이상 (영업이익은 음수 허용)
   - 시나리오 계산: 낙관 +10% / 기본 / 보수 −10%, 인건비·감가상각비 고정, 그 외 비용 매출 비례
   - 법인세: tax-rules.json의 누진 구간(2026년 10/20/22/25%) + 지방소득세 10%
   - 부가세: (매출 − 매입) × 10%
   - 결과 카드 표시, LocalStorage 저장/로드 (키: 재무제표 · 절세전략 · 지원사업_검색)

## 제약 조건
- 외부 라이브러리 없음 (서체 Pretendard CDN만 허용)
- HTML·CSS·JS를 index.html 한 파일에 작성, 데이터는 인라인 블록으로 포함
- 모든 문구 한국어, 각 함수 위에 한국어 주석

## 완료 조건
1. index.html을 더블클릭으로 열면 입력폼이 보임
2. 숫자 입력 후 "분석 시작" → 결과 카드 표시
3. F12 콘솔 JavaScript 오류 0개
4. 새로고침 후에도 입력값 유지
5. "데이터 초기화" 버튼으로 저장 데이터 삭제 가능

## 마지막으로
- 전체 파일을 출력하지 말고, 파일을 저장한 뒤 "추가·변경된 부분"을 섹션별로 요약
- 주요 함수 3개(검증, 시나리오 계산, 법인세 계산)를 비전공자에게 설명
- DEVELOPMENT_LOG.md에 오늘 날짜로 기록
```

## 🔧 4단계: 기능 추가 프롬프트 (패턴)
> 기존 기능을 보호하면서 추가하는 방식. 예시는 이미 구현된 "절세전략 추천"을 다시 요청하는 형태.

```
CLAUDE.md 규칙에 따라 STEERING.md·SPECS.md를 읽은 뒤, index.html을 분석하고 다음 기능을 추가해 주세요.

## 현재 상태 확인
- 입력폼: ✓ 기업정보 + 재무데이터 + 내년 계획
- 3개 시나리오 계산: ✓
- 결과 카드: ✓

## 추가할 기능: 절세전략 추천 (SPECS.md Task 10)
추가 위치: 3번 탭 "절세전략" 패널 (#panel-tax)
규칙 파일: docs/tax-rules.json 의 값만 사용 (숫자 하드코딩 금지)
제시할 전략:
- 통합고용세액공제 — 내년 상시근로자 증가 시, 1인당 정액(수도권 외 청년등 1,000만원·그 외 700만원, 3년 누계 표시)
- 통합투자세액공제 — 설비투자 계획액 × 10% + 3년 평균 초과분 × 10% (토지·건물 제외, 신규 투자액 기준)
- 근로소득증대세제 — 임금 인상률이 최근 평균 인건비 증가율을 넘을 때 초과분 × 20%
각 전략마다:
- "적용 가능" / "적용 불가" 배지, 불가 시 행동 힌트("1명 채용 시 ○○만원")
- 절감 예상액 (원 단위, 천단위 쉼표)
- "자세히 보기" 클릭 시 조건·계산 근거·주의 (collapse)

## 규칙
- 기존 입력폼·시나리오 계산·결과 카드 삭제 금지
- LocalStorage 저장 방식 유지 (절세 결과는 "절세전략" 키)
- 완료 후 "어떤 부분이 추가되었는지" 요약 (전체 파일 출력 금지)

## 완료 후
1. 변경 부분 요약 + 절세전략 계산 함수 로직 설명 (한국어)
2. 테스트: 샘플 데이터(광주 제조업)로 3개 전략이 올바르게 표시되는지 확인
3. DEVELOPMENT_LOG.md 갱신
```

## 🔑 5단계: 기업마당 Open API 인증키 발급 프롬프트 (브라우저 도구 사용)
> 전제: Claude Code에 브라우저 제어 도구(Claude in Chrome 확장 또는 Playwright MCP)가 연결되어 있고, **사용자가 먼저 브라우저에서 기업마당(https://www.bizinfo.go.kr)에 로그인**해 둔 상태.
> 회원가입·본인인증·비밀번호 입력은 자동화 대상이 아닙니다 (사용자가 직접).

```
당신은 텍스내비 프로젝트의 개발 담당 AI입니다. CLAUDE.md·STEERING.md 규칙을 따릅니다.

## 작업: 기업마당 "지원사업정보 Open API" 인증키 신청 → 텍스내비에 적용
전제 확인: 먼저 브라우저에서 https://www.bizinfo.go.kr 이 로그인 상태인지 확인하세요.
로그인되어 있지 않으면 작업을 멈추고 저에게 로그인을 요청하세요. 아이디·비밀번호를 대신 입력하지 마세요.

절차:
1. 기업마당 > 활용정보 > 정책정보 개방 > "지원사업정보 API" 상세 페이지로 이동
   (https://www.bizinfo.go.kr/web/lay1/program/S1T175C174/apiDetail.do?id=bizinfoApi)
2. 페이지에서 다음을 확인해 docs/bizinfo-api.md 에 기록하세요:
   요청 URL, 인증키 파라미터명(crtfcKey), 요청 파라미터(dataType, searchCnt, searchLclasId, hashtags, pageUnit, pageIndex),
   응답 필드(pblancNm, jrsdInsttNm, excInsttNm, reqstBeginEndDe, pldirSportRealmLclasCodeNm, hashtags, pblancUrl, bsnsSumryCn, trgetNm).
   index.html 의 함수 `기업마당_불러오기`·`공고정규화` 가 쓰는 이름과 다르면 차이를 알려주고 수정을 제안하세요.
3. "인증키 신청(발급)" 버튼을 눌러 신청서를 작성하세요.
   - 이용 목적 예시: "광주·전남 중소기업 대상 재무분석·절세 웹서비스(텍스내비)에서 지원사업 공고를 조회·표시"
   - 이미 로그인 계정 정보로 채워진 항목만 진행하고, 새로 입력해야 하는 개인정보(전화번호 등)는 저에게 물어보세요.
4. 제출 후 상태(즉시 발급 / 승인 대기)를 보고하세요. 승인 대기면 확인 위치(마이페이지 등)와 예상 소요 기간을 알려주세요.
5. 인증키가 발급되면 화면·채팅·문서에 키 값을 출력하지 말고,
   index.html 지원사업 탭의 "기업마당 Open API 인증키" 입력란에 붙여 넣는 방법만 안내하세요.
   (키는 브라우저 LocalStorage "지원사업_검색"에만 저장됩니다. HTML 코드에 넣으면 파일을 여는 누구나 볼 수 있습니다.)
6. 테스트: 지원사업 탭 > "기업마당에서 불러오기" 클릭.
   CORS 오류로 실패하면 콘솔 메시지를 보고하고, SPECS.md 향후 과제(Task 14: 서버 프록시)에 해결 방법을 적어 주세요.

규칙: 기존 기능 삭제 금지, 변경 부분 요약, 인증키를 DEVELOPMENT_LOG.md 등 어떤 파일에도 기록하지 말 것.
```

## 📌 Claude Code 사용 시 주의사항

### ✅ 좋은 패턴
| 상황 | 좋은 방식 | 이유 |
|---|---|---|
| 파일 수정 | "index.html의 `#panel-analysis` 섹션 아래 분기표 카드 추가" | 위치 명확 |
| 오류 수정 | "F12 콘솔에 `계산_시나리오 is not defined` 오류. 함수 정의 위치 확인" | 구체적 증상 |
| 기능 추가 | "현재 폼은 유지하고, 결과 카드 아래에만 추가" | 기존 보호 명시 |
| 검증 | "수정 후 새로고침해서 LocalStorage 저장 동작 확인, `python tools/test-e2e.py` 실행" | 재현 가능 |
| 데이터 수정 | "docs/tax-rules.json 세율 수정 후 `node tools/inline-data.js` 실행" | 인라인 동기화 |

## 🎯 실행 순서 (처음부터 끝까지)

### Day 1: 개인 실습
1. 프로젝트 폴더 `텍스내비` 준비 (데스크탑)
2. `CLAUDE.md` · `STEERING.md` · `SPECS.md` 작성 (한 번만) — 이후 프롬프트에서는 "STEERING.md 참조"
3. VS Code에서 폴더 열기 → Claude Code 실행 (`CLAUDE.md`가 자동으로 읽힘)
4. 3단계 프롬프트로 `index.html` 생성 (또는 기존 index.html에 4단계 패턴으로 기능 추가)
5. 브라우저에서 즉시 테스트: 더블클릭 → F12 콘솔 → 입력 → 계산 → 새로고침 유지 → 초기화
6. `DEVELOPMENT_LOG.md` 기록
   ```markdown
   ## [2026-09-03] 1차 구현 완료
   - 입력폼: 기업정보 + 재무데이터 + 내년 계획
   - 시나리오: 3개 계산 완료 (법인세 2026 누진세율)
   - 저장: LocalStorage 키 3개
   - 테스트: 콘솔 오류 없음 ✓
   ```

### Day 2: 팀 개발
1. 2일차 팀 가이드 자료(별도 제공) 참조
2. Steering + Specs를 팀 규칙으로 확장
3. EC2 서버 배포 + 기업마당 API 프록시(CORS 해결) — SPECS.md Task 13~14
---

## 🛠 수정 이력 (원안 → 개정판, 2026-09-03)
| # | 원안 | 문제 | 개정 |
|---|---|---|---|
| 1 | "STEERING.md는 Claude Code의 컨텍스트 프로토콜(MCP) 방식" | MCP는 외부 도구 연결 규약. 자동 로드 파일은 `CLAUDE.md` | `CLAUDE.md` 추가, 문구 정정 |
| 2 | "완성 후 index.html 전체 파일을 보여주세요" | 토큰 절약 팁과 모순 | "변경 부분 요약"으로 교체 |
| 3 | 폴더 `solberton-tax-nav` | 요청과 불일치 | `텍스내비` |
| 4 | `TEAM_GUIDE.html` 참조 | 폴더 구조에 없음 | "2일차 팀 가이드 자료(별도 제공)" |
| 5 | 단일 HTML + `docs/*.json` fetch | `file://`에서 CORS로 JSON 로딩 불가 | 데이터 인라인 + `tools/inline-data.js` 동기화 |
| 6 | "Dart API" / "기업마당 API(동기식)" | DART는 상장기업 공시용, 동기 XHR 폐기 예정 | 기업마당 Open API(비동기 fetch) + 실패 시 샘플 대체 |
| 7 | 외부 라이브러리 금지 ↔ Pretendard | 서체 CDN 필요 | 서체만 예외로 명시 |
| 8 | 법인세 = 영업이익 × 25% | 2026년 세율 10/20/22/25% + 지방소득세 10% | 누진 계산 + 지방소득세, 추정치 표기 |
| 9 | 부가세 = 매출 × 10% | 매출세액일 뿐, 납부세액은 매입세액 차감 | 매입액 입력 추가 → (매출 − 매입) × 10% |
| 10 | "인정상여금 세제혜택 × 20%" | 인정상여는 불이익 개념 | **근로소득증대세제** (임금증가분 × 20%) |
| 11 | "통합고용세액공제 × 10%" | 비율 아닌 1인당 정액(3년) | 2026 개편 정액표 적용, 인원 입력 추가 |
| 12 | "유형자산 > 5억 → 설비투자 × 3%" | 신규 투자액 기준, 중소기업 기본 10% | 통합투자세액공제 10% + 추가공제, 투자 계획액 입력 |
| 13 | 색상 파란색 #203a5f / 자주색 #7d6bd1 | 요청한 디자인(오렌지+쿨그레이)과 충돌 | 디자인 토큰 교체, 작은 글씨 #CC4D00 |
| 14 | 시나리오 비용 가정 없음 | 계산 불가 | 인건비·감가상각비 고정, 그 외 매출 비례 |
| 15 | 입력 단위 미정 | — | 백만원 |
| 16 | 업종·데이터 미정 | — | 6대 업종 + 샘플(가상) 데이터, 화면에 표기 |
| 17 | 분기 산출 방식 미정 | — | 연간 ÷ 4 + 부가세 신고 일정 |
| 18 | 탭/스텝, 서비스명, 저장 시점 미정 | — | 탭+진행표시, "텍스내비", 입력 즉시 자동 저장 |
| 19 | 구현 범위 | — | 1차+2차(Task 1~12) 한 번에 |
