# DEVELOPMENT_LOG — 택스네비

> 매 작업 후 날짜 · 변경 내용 · 검증 결과를 이 파일 맨 아래에 추가합니다.

## [2026-09-03] 프롬프트 문서 검토 → 결정 → 1차+2차 구현 완료

### 1) 원안 프롬프트 문서 검토 결과 (19건)
**A. Claude Code 사용법**
1. "STEERING.md는 컨텍스트 프로토콜(MCP) 방식" → 사실 오류. MCP는 도구 연결 규약. 자동 로드 파일은 `CLAUDE.md` → **CLAUDE.md 추가**
2. "전체 파일을 보여주세요" ↔ "전체 파일 재작성 금지" 모순 → **변경 요약으로 교체**
3. 폴더명 `solberton-tax-nav` ↔ 요청 `택스내비` → **`택스네비`로 통일**(사용자 확정 표기)
4. `TEAM_GUIDE.html`이 구조에 없음 → "2일차 자료(별도 제공)"로 문구 수정

**B. 기술 모순**
5. 단일 HTML ↔ `docs/*.json` fetch — `file://`에서 CORS 차단 → **데이터 인라인 + tools/inline-data.js**
6. "Dart API"(상장기업 공시용) ↔ "기업마당 API", 동기식 XHR → **기업마당 Open API(비동기) + 샘플 대체**
7. 외부 라이브러리 금지 ↔ Pretendard → **서체 CDN만 예외**

**C. 세무 오류 (2026년 기준 웹 확인)**
8. 법인세 영업이익 × 25% → 2026년 10/20/22/25% 누진 + 지방소득세 10% (법인세법 §55, 법률 제21217호 2025.12.23 개정)
9. 부가세 매출 × 10% → 매출세액 − 매입세액
10. "인정상여금 세제혜택 × 20%" → 인정상여는 불이익 개념. 정식 명칭 **근로소득증대세제**(조특법 §29의4, ~2028)
11. 통합고용세액공제 "× 10%" → 1인당 정액 3년 (2026 개편: 수도권 외 중소 청년등 1,000/1,900/2,000만원, 그 외 700/1,200/1,300만원 — 조특법 §29의8, 법률 제21223호)
12. "유형자산 > 5억 → × 3%" → 통합투자세액공제는 **신규 투자액** 기준, 중소기업 기본 10% + 3년 평균 초과분 10% (조특법 §24)

**D. 디자인 충돌**
13. 파란색 #203a5f / 자주색 #7d6bd1 ↔ 요청한 오렌지+쿨그레이 → 토큰 교체. #FF6000은 작은 글씨 대비 부족(≈3:1) → 작은 글씨 #CC4D00

**E. 미정의 → 질문으로 해결**
14~19. 시나리오 비용 가정, 입력 단위, 업종·데이터, 분기 산출, 탭/서비스명/저장 시점, 구현 범위

### 2) 사용자 결정 사항 (질문 18개 답변)
| 항목 | 결정 |
|---|---|
| Claude Code 관련 3건 | 모두 반영 (CLAUDE.md, MCP 문구, 변경 요약) |
| 데이터 파일 | index.html 인라인, JSON은 원본 보관 |
| API | 기업마당 API 연동 시도 + 실패 시 mock 대체 (Dart 삭제) |
| 폴더 | 데스크탑/택스네비 새로 생성 (홈의 기존 taxnavi는 건드리지 않음) |
| 법인세 | 2026-09-03 기준 국세청·법인세법 세율 (10/20/22/25% + 지방소득세) |
| 부가세 | 매입액 입력 추가 → (매출 − 매입) × 10% |
| 절세전략 | 실제 제도로 교정 + 입력란 추가 (증감 인원·청년등·설비투자액, 임금 인상률) |
| 시나리오 | 고정비(인건비·감가상각비) 고정, 변동비 매출 비례 |
| 입력 단위 | 백만원 |
| 업종·데이터 | 6대 업종 + 가상 데이터(화면에 '샘플' 표기) |
| 분기 | 연간 ÷ 4 균등 + 부가세 신고 일정 |
| 범위 | 1차 + 2차(Task 1~12) 한 번에 |
| 서체·대비 | Pretendard CDN 허용 + 작은 글씨 #CC4D00 |
| 서비스명 | **택스네비** (폴더명도 동일) |
| 화면 이동·저장 | 탭 + 번호·완료 표시, 입력 즉시 자동 저장 |
| API 인증키 | 기업마당 Open API. 로그인은 사용자, 신청은 Claude Code → PROMPT_GUIDE.md 5단계 프롬프트 |

### 3) 구현 내용
- `index.html` (약 96KB, 외부 라이브러리 0): 탭 4개, 입력 폼(기업정보·3개년 표·내년 계획), 시나리오 3개, SVG 차트 3개, 분기표, 업종 비교, 절세전략 3종(collapse), 지원사업(필터·D-day·기업마당 API·샘플 대체), CSV 내보내기, 인쇄 CSS, 자체 다이얼로그·토스트, LocalStorage 키 3개
- 데이터: `sample-data.json`(광주 제조업 샘플), `docs/tax-rules.json`(2026 세율·공제 + 출처), `docs/industry-avg.json`(6업종 샘플), `docs/government-support.json`(샘플 공고 12건, 기업마당 API 필드명과 동일)
- 문서: `CLAUDE.md`, `STEERING.md`(개정), `SPECS.md`(개정), `PROMPT_GUIDE.md`(개정 + 5단계 프롬프트 + 수정 이력), `README.md`
- 도구: `tools/inline-data.js`(JSON → index.html 인라인 동기화), `tools/test-e2e.py`(Playwright 자동 검증)
- 설계상 추가 결정(사용자 확정 사항에서 자연히 따라온 것): 영업이익 음수(적자) 입력 허용 / 임금 인상률 입력란(근로소득증대세제 계산에 필수) / 과거 투자액은 "유형자산 증가분 + 감가상각비"로 추정 / 공제는 법인세에서만 차감(지방소득세 그대로) / 공제액이 법인세를 넘으면 이월 안내

### 4) 검증 결과
- `tools/test-e2e.py` **49개 검사 전부 통과**: 초기 화면, 샘플 로딩, 계산값(기본 매출 51억원·세부담 6,160만원·부가세 1.95억원, 낙관 영업이익 5.73억원, 통합고용 1,700만원/3년 8,100만원, 통합투자 4,125만원, 근로소득증대 122만원), 차트 3개, 분기표, 업종비교 4건, 지원사업 필터·정렬·마감숨김·API 오류 처리, 새로고침 유지, 저장 키 3개, 입력 검증, 결손 시 법인세 0, CSV(BOM), 320/768/1920px 가로 스크롤 없음, 초기화, **JS 오류 0개**
- 참고: 검증 환경에서 폰트 CDN·기업마당 API는 네트워크 차단으로 실패 → 예상대로 시스템 폰트·샘플 데이터로 대체됨 (JS 오류 아님)
- 샘플 계산 검산: 영업이익 380백만원 → 법인세 200×10% + 180×20% = 56백만원, 지방소득세 5.6 → 합계 61.6 ✓ / 부가세 (5,100 − 3,150) × 10% = 195 ✓

### 5) 남은 과제
- SPECS.md Task 13~18: 기업마당 인증키 발급·실호출 테스트, EC2 프록시(CORS), 업종 평균·공고 실데이터 교체, 세무사 검토, (선택) JSON 백업·다크 모드
- 디자인 시스템 서체(Pretendard)는 온라인에서만 로드됨 — 오프라인 시연 시 시스템 폰트로 보임

## [2026-09-03] 대시보드 리디자인 + 기업마당 API 프록시(Vercel) + 배포

### 1) 배경
사용자 요청: "데스크탑 택스네비를 기준으로 웹을 디자인하고 안에 API를 넣어서 프로젝트를 실행" (기존 인수인계 프롬프트 이어받음).
brainstorming 스킬로 5개 질문 확인 → 설계서(`docs/superpowers/specs/2026-09-03-dashboard-redesign-design.md`) 승인 → writing-plans로 계획서(`docs/superpowers/plans/2026-09-03-dashboard-redesign.md`) 작성 → executing-plans로 구현.

### 2) 사용자 결정 사항
| 항목 | 결정 |
|---|---|
| 디자인 | 전면 리디자인 → 전문가용 대시보드(좌측 사이드바 내비, 표·차트 밀도 높음) |
| 색상 테마 | 다크 사이드바(gray-900) + 라이트 본문, 오렌지는 액센트로 유지 (새 hex 없음) |
| 메뉴 구조 | 개요 화면 추가 → 5개(개요·입력·분석결과·절세전략·지원사업) |
| 구현 방식 | 단일 `index.html` 유지, CSS·HTML 교체, JS 계산 함수([JS 1]~[JS 8]) 그대로 재사용 |
| API 실행 | Vercel 서버리스 함수 `api/bizinfo.js` 1개(외부 패키지 0), 로컬 `vercel dev`, 배포 `vercel --prod` |
| 인증키 | 미보유 → 발급 절차 안내 완료(README·화면 링크), 사용자 발급 후 실호출 예정 |
| 검증 | Playwright + Chromium 신규 설치, `tools/test-e2e.py` 전면 갱신 |

### 3) 구현 내용
- **`api/bizinfo.js`** (신규, 40줄, 외부 패키지 0): 기업마당 지원사업정보 Open API 프록시. 허용 쿼리 키만 패스스루(호스트 고정), 키 없음 400, 상위 실패 502, 오류 로그는 인증키 마스킹. `vercel.json`(maxDuration 10) · `.vercelignore` 신규
- **`index.html`** (약 125KB→118KB, 외부 라이브러리 0 — Pretendard CDN만 예외 유지): 상단 탭 4개 → **좌측 사이드바(다크) + 라이트 본문 + 모바일 하단 탭바**, 화면 5개(**개요 신규** + 입력·분석결과·절세전략·지원사업). 카드 나열 → **표 중심**(시나리오 비교표, 업종 비교표, 절세전략 표+확장행, 지원사업 표)으로 밀도 상향. 입력 화면에 진행률(n/18) sticky 요약 패널 추가. `기업마당_불러오기`가 `http(s)://`면 `/api/bizinfo` 프록시, `file://`면 기존 직접 호출로 분기. 계산 함수(`계산_법인세`·`계산_시나리오`·`계산_절세전략`·`계산_업종비교`)는 무변경
- **`docs/tax-rules.json`**: `법인세.신고일정`(확정 신고 3/31, 중간예납 8/31) 추가 → 개요 화면 "다가오는 세금 일정"에 사용. `node tools/inline-data.js`로 동기화
- **`tools/test-e2e.py`** 전면 교체: `TEST_URL` 환경변수 지원(file:// 기본 / localhost / 배포 주소), 새 화면 선택자(`#nav-*`·`#mnav-*`·`#scenario-switch`·`#scenario-table` 등), 개요 화면 검사 6건, 프록시 검사 2건(서버 URL일 때만), 인쇄 CSS 검사, 태블릿 1024px 스크린샷 추가
- 문서: `STEERING.md`(2·4·8항 개정), `SPECS.md`(2항 다이어그램 교체, Task 14 완료 처리, Task 19 추가), `README.md`(3가지 실행 방법, 화면 5개 표), `CLAUDE.md`(서버 포함 검증 명령 추가)
- 설계·계획 문서: `docs/superpowers/specs/2026-09-03-dashboard-redesign-design.md`, `docs/superpowers/plans/2026-09-03-dashboard-redesign.md`

### 4) 검증 결과
- `python3 tools/test-e2e.py` (file://) **66개 검사 전부 통과**, JS 오류 0개
- `TEST_URL=http://localhost:3000 python3 tools/test-e2e.py` (`vercel dev`) **68개 검사 전부 통과** — 프록시 2건(키 없음 400 / 잘못된 키 시 기업마당의 `{"reqErr":"존재하지 않는 인증키 입니다."}` 그대로 수신 확인) 포함
- `TEST_URL=https://taxnavi-dashboard.vercel.app python3 tools/test-e2e.py` (배포 주소) **68개 검사 전부 통과**
- 계산값(회귀 없음 확인): 기본 매출 51억원 · 세부담 6,160만원 · 부가세 1.95억원, 낙관 영업이익 5.73억원, 통합고용 1,700만원/3년 8,100만원, 통합투자 4,125만원 — 기존 수치와 동일
- 320/768/1024/1920px 가로 스크롤 없음, 모바일 하단 탭바·본문 액션 표시 확인, 인쇄 CSS(사이드바 숨김+패널 5개 출력) 확인, 새로고침 유지·초기화 확인
- 스크린샷 21장 `tools/screenshots/`에 저장, 개요·분석·절세 3개 화면 육안 확인(라벨 겹침·넘침 없음)

### 5) 배포
- **배포 주소**: `https://taxnavi-dashboard.vercel.app` (Vercel 프로젝트 `taxnavi-dashboard`, GitHub 연동 없이 `vercel --prod`로 CLI 직접 업로드 — 사용자 지시로 GitHub 커밋·푸시는 하지 않음)
- 로컬 실행: `vercel dev` → http://localhost:3000

### 6) 남은 과제
- Task 13: 기업마당 API 인증키 미발급 — 사용자가 bizinfo.go.kr 로그인 → 활용정보 → 정책정보 개방 → 지원사업정보 API에서 신청 후, 지원사업 화면 "기업마당 실시간 공고 연동" 패널에 입력해 실호출 테스트 (인증키는 어떤 파일·로그에도 기록하지 않음 — 이번 세션에서도 기록 없음)
- Task 15~18: 업종 평균·지원사업 샘플 데이터를 실제 통계로 교체, 세무사 검토, 선택 사항(백업/다크모드)은 그대로 미착수
- 시나리오 선택(보수/기본/낙관)은 화면 상태로만 유지되며 LocalStorage에 저장하지 않음(새로고침 시 '기본'으로 복귀) — 의도된 단순화

## [2026-09-03] 기업마당 API 인증키 서버 등록 (Task 13 진행)

### 1) 변경 내용
사용자 요청: 화면에서 인증키를 매번 입력하지 않아도 되도록 서버에 미리 등록.
- `api/bizinfo.js`: Vercel 환경변수 `BIZINFO_API_KEY`가 설정돼 있으면 그 값을 우선 사용(클라이언트가 보낸 키는 무시), 없으면 기존처럼 요청의 `crtfcKey`를 사용하도록 변경
- `index.html` `기업마당_불러오기`: http(s)로 열렸을 때(vercel dev·배포 주소) 화면에 인증키를 입력하지 않아도 "기업마당에서 불러오기" 버튼이 바로 동작. 더블클릭(file://)은 서버가 없어 여전히 화면 입력 필수. 인증키 입력란은 "서버에 등록되어 있으면 비워 두어도 됩니다"로 안내 문구 변경(입력란 자체는 삭제하지 않음 — file:// 대비 및 override 필요 시 대비)
- `tools/test-e2e.py`: 서버 모드 검증을 새 동작에 맞게 조정 — 빈 키로 눌러도 즉시 막지 않고 시도하도록, 프록시 최소 요청 검사는 `searchCnt=1`로 고정(파라미터 없이 부르면 기업마당이 전체 목록을 내려주다 8초 타임아웃(502) 발생 확인 — 실제 앱은 항상 `searchCnt=100`을 보내 4초대 정상 응답이라 영향 없음)

### 2) 인증키 등록
- 사용자가 채팅에 값을 붙여넣었고, 사용자의 반복된 명시적 요청에 따라 `vercel env add BIZINFO_API_KEY`로 Production·Preview·Development 3개 환경에 암호화 저장 (Vercel 대시보드에서 값은 `Encrypted`로만 표시됨)
- 프로젝트 파일·git·이 로그 등 어디에도 키 값을 적지 않음 (등록 후 저장소 전체 grep으로 잔존 여부 확인 — 없음)
- `vercel --prod`로 재배포하여 환경변수 반영

### 3) 검증 결과
- `curl https://taxnavi-dashboard.vercel.app/api/bizinfo?searchCnt=3` → 200, 기업마당 실제 공고 3건 정상 수신 확인
- `python3 tools/test-e2e.py` (file://) **66개 전부 통과**
- `TEST_URL=http://localhost:3000` (`vercel dev`, 키 등록 전 상태에서 먼저 검증) **68개 전부 통과**
- `TEST_URL=https://taxnavi-dashboard.vercel.app` (키 등록 후) **68개 전부 통과** — 지원사업 화면에서 인증키 입력 없이 "기업마당에서 불러오기" 클릭만으로 실제 공고 100건 수신 확인

### 4) 남은 과제
- Task 13 완료: 인증키 발급·서버 등록·실호출 검증까지 끝남
- 화면의 샘플 데이터는 여전히 기본값으로 남아 있고, 사용자가 "기업마당에서 불러오기"를 눌러야 실시간 데이터로 전환됨(자동 전환은 이번 범위 밖 — 필요하면 추가 요청)

## [2026-09-03] 절세전략 "자세히 보기"에 출처 링크 추가

사용자 요청: 절세전략 카드에서 "자세히" 눌렀을 때 뜨는 정보에 클릭 가능한 링크(출처)도 넣어달라는 요청.
- `계산_절세전략`: 통합고용·통합투자·근로소득증대 3개 전략 객체에 `출처`(URL 배열) 필드 추가 — `docs/tax-rules.json`에 이미 있던 `출처`(법령 원문 · 참고 자료 링크)를 그대로 전달, 새 데이터 추가 아님
- `렌더_절세`: "자세히 보기" 확장 영역(`<dl>`)에 "출처" 항목 추가, `<a target="_blank" rel="noopener">`로 새 탭에서 열리는 링크 2개(법령 원문 / 참고 자료) 렌더링
- 검증: `python3 tools/test-e2e.py` 66개 전부 통과, 스크린샷으로 육안 확인(통합고용세액공제 펼침 시 링크 2개 정상 표시)

## [2026-09-03] 지원사업 화면 — 수동 인증키 입력·불러오기 버튼 삭제, 자동 조회로 전환

사용자 요청: "직접 api를 칸에 입력하고 불러오기를 눌러야 기업마당 정보가 뜨는" 방식을 없애 달라는 요청. 서버에 `BIZINFO_API_KEY`가 이미 등록돼 있으므로 화면 조작 없이 자동으로 실시간 공고를 불러오도록 변경.

### 변경 내용
- `index.html`: "기업마당 실시간 공고 연동" `<details>` 패널(인증키 입력란·"불러오기"/"샘플로 되돌리기" 버튼·상태 문구) 전체 삭제, 관련 CSS(`.api-panel`, `.api-row`)도 함께 삭제
- `기업마당_불러오기()`: 인자 없이 항상 서버 등록 키로 호출하도록 단순화(더 이상 클라이언트 키를 받지 않음)
- **`지원사업_자동조회()` 신규**: 서버 모드(http/https)에서 화면 진입 시 자동 호출, 성공 시 실시간 공고로 교체·1시간 캐시, 실패 시 자동으로 샘플 대체 — 결과는 기존 `#support-source` 소스 칩("기업마당 실시간" / "샘플 데이터")으로만 표시(별도 상태 문구 없음). 더블클릭(file://)은 서버가 없어 시도하지 않고 항상 샘플 유지
- `상태.지원사업설정`에서 더 이상 쓰이지 않는 `인증키` 필드 제거(LocalStorage에도 저장 안 됨)
- `tools/test-e2e.py`: 버튼·입력란 검사를 자동조회 검증으로 교체(서버 모드는 소스 칩과 목록 표시 여부만 확인, file://는 기존 샘플 필터 로직을 그대로 상세 검증)

### 검증
- `python3 tools/test-e2e.py` (file://) **65개 전부 통과**
- `TEST_URL=http://localhost:3000` (`vercel dev`) **64개 전부 통과** — 지원사업 화면 진입만으로 "기업마당 실시간" 자동 표시 확인
- `TEST_URL=https://taxnavi-dashboard.vercel.app` (배포) **64개 전부 통과**. 수동 확인: 배포 주소에서 지원사업 화면 진입 후 실제로 약 5초 뒤(콜드스타트 포함) "샘플 데이터" → "기업마당 실시간"으로 자동 전환되는 것을 Playwright로 직접 관찰
- 문서 갱신: STEERING.md(2·6항), SPECS.md(화면 설계·요구사항), README.md(기업마당 Open API 섹션·화면 구성 표)
- `vercel --prod` 재배포 완료 (https://taxnavi-dashboard.vercel.app)

## [2026-09-03] 업종 평균을 DART(전자공시) 코스닥·코넥스 실제 재무제표로 교체 (Task 15 완료)

사용자 요청: DART API 키 제공 + "특정 업종과 같은 기업들의 재무데이터 평균을 실시간으로 가져와 비교"하고 싶다는 요청, 이어서 "코스피가 아니라 코스닥·코넥스 소프트웨어(→이후 전 업종) 기업으로" 범위를 좁힘.

### 1) 사전 확인 (실제 DART API 호출로 검증, 추측 없음)
- `DART_API_KEY`를 Vercel 환경변수 3개 환경에 등록 (BIZINFO_API_KEY와 동일 방식, 값은 어떤 파일·로그에도 없음)
- 요청하신 14개 필드를 실제 DART API(`company.json`·`fnlttSinglAcntAll.json`·`empSttus.json`)로 하나씩 확인한 결과:
  - **확실히 가능**: 기업명, 업종코드, 매출액, 영업이익, 매출증가율(계산), 유형자산, 유형자산 취득액, 상시근로자수(직원현황 API)
  - **근사 가능**: 인건비(직원현황의 "연간급여총액", 일부 대기업은 부문별 공시로 미기재), 이자비용·영업외수익("금융비용"·"금융수익"으로 근사, 순수 항목과 정확히 일치하지 않음)
  - **불가능(확인됨)**: 감가상각비(삼성전자·이마트 등 표준 API 어디에도 계정과목으로 없음, 주석에만 있음), 과세매입액·접대비·기부금(세무조정 자료라 DART 회계공시 범위 밖)
- corpCode.xml(전체 118,831개 기업, 상장 3,988개)을 직접 다운로드해 회사명→corp_code, `company.json`의 `corp_cls`(Y=코스피/K=코스닥/N=코넥스)로 코스닥·코넥스 기업만 실제로 걸러냄(암기 아닌 파일 검색)

### 2) 구현
- **`tools/update-industry-avg.js`** (신규, 외부 패키지 0): 업종별 코스닥·코넥스 대표기업(하드코딩, 실제 DART에서 확인된 corp_code) → `fnlttSinglAcntAll`(매출액·영업이익, CIS/IS 둘 다 처리) + `empSttus`(급여총액)를 가져와 업종별 영업이익률·매출증가율·인건비비율 평균을 내고 `docs/industry-avg.json` 저장. `.env.local`을 직접 파싱해 `DART_API_KEY` 로드(dotenv 등 외부 라이브러리 없음)
- 업종별 표본: 제조업 7곳(에코프로비엠·리노공업·HPSP 등 반도체 소부장), 정보통신업 8곳(안랩·웹케시·이스트소프트·컴투스홀딩스·골프존 등), 건설업 4곳, 도소매업 3곳(실리콘투 등), 서비스업 2곳(메가스터디교육·씨엔알리서치) — **음식·숙박업은 코스닥·코넥스 상장사를 찾지 못해 0곳, 기존 샘플값 유지**(스크립트가 자동으로 기존 파일값을 읽어 보존)
- `index.html`: 업종평균 출처 표시가 "샘플값"으로 하드코딩되어 있던 부분(SVG 차트 범례)을 실제 출처(`업종평균_출처문구()` 신규 함수, 표본기업 수 표시)로 교체. 계산 로직(`계산_업종비교`)은 무변경 — 데이터만 실측으로 교체되어 자동 반영
- 문서: SPECS.md Task 15 완료 처리, README.md에 갱신 스크립트 사용법 섹션 추가

### 3) 검증
- 실제 스크립트 실행 로그: 24개 기업 재무데이터 정상 수신(예: 에코프로비엠 매출 2조 7,668억원 2024년), 감가상각비율은 전 업종 `null`로 저장되어 화면에 "데이터 없음" 정상 표시
- `python3 tools/test-e2e.py` (file://) **65개 전부 통과**, 스크린샷으로 "제조업 평균 (7개사 평균, DART 코스닥·코넥스 실제 데이터)" 문구·차트·해석표 정상 확인
- `TEST_URL=https://taxnavi-dashboard.vercel.app` **64개 전부 통과**, 배포 사이트에 실데이터 반영 확인
- `vercel --prod` 재배포 완료

### 4) 한계 (화면·문서에 명시)
- 코스닥·코넥스라도 상장·공시 의무가 있는 기업 위주라 실제 "재무팀 없는 비상장 중소기업"과는 차이가 있을 수 있음 — 참고용
- 도소매업(3곳)·서비스업(2곳)은 표본이 적음
- 음식·숙박업은 실데이터 없음(샘플 유지)
- 감가상각비율은 전 업종 데이터 없음(DART 표준 API 미제공)
- 갱신은 수동 실행(주기적 캐싱, 사용자 확정 방식) — 몇 달에 한 번 또는 배포 전 `node tools/update-industry-avg.js && node tools/inline-data.js` 실행 권장

## [2026-09-03] 업종 평균 비교 진단 카드(지표 막대 + 레이더 차트) 추가

사용자 요청: "업종 평균 비교 진단[을] 분석결과 칸 위에 추가 — 매출액 증가율, 영업이익률, 이자비용, 인건비 비중을 비교하고 지표 막대로도 볼 수 있게 하고 강점·약점을 레이더로 비교".

### 변경 내용
- `재무항목`에 **이자비용**(선택 입력, 3개년) 추가 — 재무 표·검증·저장·CSV·샘플불러오기 등 `재무항목` 배열을 순회하는 기존 코드가 자동으로 이 필드도 처리(재사용, 신규 코드 최소화)
- `계산_진단비교()` 신규: 매출증가율·영업이익률·이자비용비율(이자비용÷매출액)·인건비비율 4개 지표로 우리 회사 vs 업종 평균 데이터 구성. 기존 `계산_업종비교()`(감가상각비율 포함 4지표, 화면 하단 상세 비교용)는 그대로 유지
- `레이더차트()` 신규 함수: 외부 라이브러리 없이 SVG로 방사형 차트 직접 구현. 지표마다 단위·범위가 달라(매출증가율 −10~30%, 인건비비율 10~30% 등) 축마다 독립 스케일링(두 값 중 큰 쪽 기준)해 강점·약점이 왜곡 없이 보이도록 함. 색상은 기존 토큰 그대로(우리=오렌지, 업종평균=회색), 마우스 오버 툴팁은 기존 `툴팁연결()` 재사용
- 지표 막대는 기존 `가로쌍막대차트()`를 그대로 재사용(신규 코드 없음)
- **분석결과 화면 맨 위**(KPI 스트립보다 위)에 "업종 평균 비교 진단" 카드 신규 배치 — 지표 막대 + 레이더 2열 그리드, 이자비용 미입력 시 안내 문구 표시
- `docs/industry-avg.json`·`tools/update-industry-avg.js`: **이자비용비율**(금융비용÷매출액 근사) 계산 추가 — 기존에 이미 가져오던 재무제표 응답에서 "금융비용" 계정을 추가로 뽑아내는 것이라 API 호출 추가 없음. 제조업(1.6%)·서비스업(0.6%)·건설업(1.4%)·정보통신업(4.5%) 4개 업종은 값 확보, 도소매업·음식·숙박업은 데이터 없음(화면에 "데이터 없음" 표시)
- `sample-data.json`에 이자비용 샘플값(15/18/20백만원) 추가해 샘플 데이터로도 바로 시연되게 함
- 업종평균 출처 표시 하드코딩("샘플값" 고정)을 실제 출처로 고치는 김에 `업종평균_출처문구()` 함수로 통합해 지표 막대·레이더·업종비교 3곳에서 공용으로 사용

### 검증
- `python3 tools/test-e2e.py` (file://) **66개 전부 통과** — 신규 검사 2건(SVG 5개, 레이더에 4개 지표 텍스트 존재) 포함
- 스크린샷으로 데스크톱·모바일(375px) 모두 확인 — 레이더 축 라벨 겹침 없음, 모바일에서 막대→레이더 세로 스택 정상
- `TEST_URL=https://taxnavi-dashboard.vercel.app` **65개 전부 통과**
- `vercel --prod` 재배포 완료

## [2026-09-03] 레이더 차트 여백 조정 — 값이 테두리 끝까지 닿던 문제 개선

사용자 요청: "강세 약세 레이더표가 퍼센트로 표시하다보니 레이더 끝까지 닿아서 보기 좋지 않다"는 피드백.
- 원인: 축마다 우리 회사·업종 평균 중 큰 값을 그대로 반지름 100%(테두리)에 매핑해서, 두 값 중 하나는 항상 테두리에 붙어 그려짐
- 수정: `레이더차트()`의 축범위 계산에서 실제 최댓값에 25% 여유를 둬(`hi = lo + (실제최대 − lo) × 1.25`) 값이 반지름의 최대 80% 지점까지만 채워지도록 변경. 값들 사이의 비율(상대적 크기 차이)은 그대로 유지되고, 절대적으로 테두리에 닿지 않아 보기 편해짐
- 검증: `python3 tools/test-e2e.py` 66개 전부 통과, 스크린샷으로 여백 확보 확인, 배포(`https://taxnavi-dashboard.vercel.app`) 65개 통과

## [2026-09-03] 세금 일정 — 주말·공휴일이면 다음 평일로 자동 연장 (국세기본법 제5조)

사용자 요청: "세금 일정을 보면 날짜는 정확한데 그 해당날이 주말 및 공휴일 등 경우 다음날로 연장되는 걸 반영해줘".

### 근거·데이터 확인
- 국세기본법 제5조(기한의 특례): 신고·납부기한이 공휴일·토요일·일요일이면 다음날을 기한으로 함
- 실제 공휴일 날짜는 웹 검색으로 확인(추측 금지) — 2026·2027년은 publicholidays.co.kr 공식 표를 그대로 반영(대체공휴일 포함), 2025년은 정부 발표 임시공휴일(1/27) 포함해 반영. 제헌절(7/17)은 2008년 이후 법정 공휴일이 아니라 목록에서 제외

### 구현
- `docs/tax-rules.json`에 `공휴일` 섹션 신규 추가 — `목록`(2025~2027년 "YYYY-MM-DD" 58건), `근거`·`출처`·`확인일`, 그리고 "이후 연도는 주말만 자동 판정되고 공휴일은 매년 갱신 필요"라는 한계 명시
- `index.html`: `공휴일_집합`(Set) · `다음영업일(d)`(토·일·공휴일이면 평일이 나올 때까지 하루씩 미룸, 대체공휴일 연쇄도 처리) · `날짜문자열()` 신규. `다가오는일정()`이 각 항목의 원래 날짜와 연장된 날짜를 함께 들고 있다가(`연장됨` 플래그), 개요 화면 "다가오는 세금 일정" 카드에 실제(연장된) 날짜로 D-day를 계산하고 연장된 경우 "원래 YYYY.MM.DD이 주말·공휴일이라 다음 평일로 연장" 안내를 덧붙임
- 분석결과 화면 분기표의 "4월 25일" 같은 일반 설명 문구는 특정 연도에 매이지 않아 연장 반영 대상에서 제외(의도적 — STEERING.md에 명시)

### 검증
- Node 자체 검증 4건(평범한 주말→월요일, 대체공휴일 연쇄로 2일 연속 미뤄지는 케이스, 평일은 그대로, **실제 다가오는 부가세 신고일 2026-10-25(일)→10-26(월) 자동 연장** 확인) 전부 통과
- `python3 tools/test-e2e.py` (file://) 66개 전부 통과, 스크린샷으로 화면에 "원래 2026.10.25이 주말·공휴일이라 다음 평일로 연장" 문구 정상 표시 확인
- 배포(`https://taxnavi-dashboard.vercel.app`) 65개 통과

### 한계
- 공휴일 목록은 2025~2027년만 반영 — 이후 연도는 주말만 자동 판정되고 실제 공휴일(특히 음력 설날·추석)은 반영되지 않음. 매년 `docs/tax-rules.json`의 `공휴일.목록`에 다음 해를 추가해야 함(수동 갱신, 문서에 안내)

## [2026-09-03] 소재지를 전남광주통합특별시 27개 구·시·군으로 세분화

사용자 요청: "소재지 입력할 때 이번에 전남광주통합특별시로 변경되었는데 나머지 군 단위도 알아서 적용해" → 확인 질문에 "전남통합특별시에 해당되는 곳만 소재지 나오게 해줘"로 답변.

### 사전 확인 (웹 검색, 추측 없음)
- 2026-07-01 광주광역시+전라남도가 "전남광주통합특별시"로 통합(특별법 2026-03-05 공포) — Wikipedia로 구성 확인: 광주 5개 구(동·서·남·북·광산) + 전남 5개 시(목포·여수·순천·나주·광양) + 전남 17개 군(담양·곡성·구례·고흥·보성·화순·장흥·강진·해남·영암·무안·함평·영광·장성·완도·진도·신안) = 27개

### 구현
- `index.html` `#f-region` select: 기존 "광주광역시/전라남도" 2개 → optgroup 3개(광주 5구/전남 5시/전남 17군)로 27개 옵션 세분화. "기타 (수도권 외)"·"수도권"은 그대로 유지(범위 밖 지역용)
- `소재지이름` 27개 신규 항목 추가, `소재지_지역필터`는 광주 5구→'광주', 전남 22개(5시+17군)→'전남'으로 매핑해 지원사업 지역 필터·government-support.json의 기존 region 체계와 그대로 호환(코드 변경 없이 재사용)
- 수도권 세액공제 판정(`기.소재지 === 'capital'`)은 등식 비교라 새 코드 27개 전부 자동으로 "수도권 외"로 처리 — 로직 변경 불필요
- **옛 저장값 마이그레이션**: `소재지_옛코드변환`(gwangju→gwangsan, jeonnam→mokpo) 추가, `불러오기_재무제표()`에서 LocalStorage를 읽을 때 자동 변환 — 기존 사용자의 저장된 데이터가 깨지지 않음
- 기본값·샘플 데이터의 소재지를 `gwangju` → `gwangsan`(광산구)으로 교체

### 검증
- `python3 tools/test-e2e.py` (file://) 66개 전부 통과(기존 "지원사업 지역 필터 기본 광주" 검사도 그대로 통과 — gwangsan→'광주' 매핑 정상)
- Playwright로 드롭다운 옵션 29개(27개 구체 지역 + 기타 + 수도권) 정확히 렌더링 확인, 샘플 데이터 로드 시 "광산구" 정상 선택 확인
- 배포(`https://taxnavi-dashboard.vercel.app`) 65개 통과

## [2026-09-03] 기준연도 직접 입력 전환 + 재무데이터 "직접 입력/자동 입력" 세그먼트

사용자 요청: "입력 항목에서 기준연도를 선택하는게 아니라 기준연도를 직접 입력할 수 있게 해주고 밑에 재무데이터도 직접 입력하는 칸이 자동 입력하는 칸으로 선택할 수 있게 해줘". 확인 질문에 "자동 입력 = 기존 샘플 데이터 불러오기와 동일"로 답변.

### 변경 내용
- `#f-base-year`: `<select>`(2024/2025/2026 3개만) → `<input type="text" inputmode="numeric">` 직접 입력으로 교체. 숫자만 입력되도록 정리, `검증_입력()`에 범위 검사(2015~2035) 추가, `입력완료여부()`도 이 범위를 함께 확인. `폼읽기()`는 빈 값·잘못된 값이면 `null`로 안전 처리(기존 `연도목록()`/`예측연도()`의 `|| 2025` 기본값 로직 재사용, 새 코드 없이 안전)
- 재무데이터 카드 헤더 아래 "직접 입력 / 자동 입력(샘플)" 세그먼트(`#fin-mode-switch`, 기존 `.segmented` 스타일 재사용) 신규 추가 — "자동 입력" 클릭 시 기존 `샘플불러오기()`를 그대로 호출(신규 로직 없음, 재사용). "직접 입력"은 상태 표시만 바꾸고 입력값은 건드리지 않음(자동으로 채운 값도 이후 자유롭게 고칠 수 있음, 입력칸을 잠그지 않는 의도적 단순화)
- 사이드바의 기존 "샘플 데이터 불러오기" 버튼과 세그먼트가 같은 함수를 호출하므로 어느 쪽으로 불러도 세그먼트 상태가 "자동 입력"으로 맞춰짐. "입력값 지우기" 시에는 "직접 입력"으로 복귀

### 검증
- `python3 tools/test-e2e.py` (file://) **70개 전부 통과** — 신규 검사 4건(기준연도 입력칸 값, 자동 입력 세그먼트 선택 상태, 직접 입력 전환, 기준연도 변경 시 연도 헤더 갱신) 포함
- 스크린샷으로 화면 확인 — 기준연도 입력칸·세그먼트·이자비용 입력칸 모두 정상 표시
- 배포(`https://taxnavi-dashboard.vercel.app`) 69개 통과

## [2026-09-03] 시나리오를 기본 1개로 단순화 + 소재지에서 기타/수도권 삭제

사용자 요청: "시나리오 비교에 보수 기본 낙관이 있었잖아 보수 낙관 없애고 기본으로만 예측을 할거니까 보수와 낙관이랑 관련된 부분은 없애 소재지에 기타랑 소재지 항목도 삭제해" (후자는 문맥상 "기타"·"수도권" 옵션 삭제로 해석).

### 변경 내용 — 시나리오 단순화
- `시나리오정의`: 보수(-10%)/기본(0%)/낙관(+10%) 3개 → **기본(0%) 1개**로 축소(`계산_시나리오()`는 이미 배열을 순회하는 범용 코드라 내부 로직 변경 없이 자동으로 1개짜리 결과를 만들어냄 — 재사용)
- 툴바 "보수/기본/낙관" 세그먼트(`#scenario-switch`) 전체 삭제(HTML·바인딩·표시 로직), 이제 전환할 시나리오가 없으므로 `렌더_시나리오의존()` 등 관련 죽은 코드도 함께 삭제
- 분석결과 화면 "시나리오 비교표" → 열이 실적+예측 2개로 축소, 카드 제목 "OOOO년 시나리오 비교 — 보수·기본·낙관" → "OOOO년 실적 대비 예측"으로 변경
- 개요 화면의 "시나리오 비교" 카드(미니표+막대) 전체 삭제 — 1개짜리 "비교"는 의미가 없어짐. 남은 3장(다가오는 세금 일정·임박한 지원사업·업종 평균 대비)을 `.grid--3`로 재배치해 한 줄에 표시
- KPI 라벨 "${시나리오명} 시나리오 예상 매출" → "예상 매출", 차트·카드 힌트의 "내년 시나리오" 문구 → "내년 예측"으로 정리
- `상태.선택시나리오` 상태 필드·`#overview-scn-table`/`#overview-bars`용 CSS(`.bars*`) 등 더 이상 쓰이지 않는 코드 삭제(재사용 없이 죽은 코드로 남기지 않음)
- `계산_절세전략()`의 `분석.시나리오[1]`(기존에 "기본"을 가리키던 인덱스) → `분석.시나리오[0]`

### 변경 내용 — 소재지
- `#f-region`에서 "기타 (수도권 외)"·"수도권 (서울·인천·경기)" 옵션 삭제 — 전남광주통합특별시 27개 구·시·군만 남김(이 서비스는 전남광주 기업 전용이라는 정체성에 맞춤)
- `소재지이름`·`소재지_지역필터`에서 `other`/`capital` 항목 삭제. 수도권 세액공제 우대 판정(`기.소재지 === 'capital'`)은 등식 비교라 코드 변경 없이 항상 false(수도권 외 우대 적용)로 자연스럽게 귀결

### 검증
- `python3 tools/test-e2e.py` (file://) **67개 전부 통과** — 시나리오 관련 검사 재작성(예측 표 열 1개, 툴바 세그먼트 삭제 확인 등), 낙관/보수 관련 검사 삭제
- 스크린샷으로 분석결과·개요 화면 확인 — 시나리오 세그먼트 없음, KPI·차트·업종 진단 카드 정상 표시, 개요 3열 재배치 정상
- 배포(`https://taxnavi-dashboard.vercel.app`) 66개 통과

## [2026-09-03] 업종 평균 비교 진단 — 차트 범례 문구 축약

사용자 요청: "분석결과 → 업종 평균 비교 진단 부분에 '업종 평균(제조업, 7개사 평균, DART 코스닥·코넥스 실제 데이터)'라고 써져있는데 '(DART 코스닥·코넥스 실제 데이터)'로 변경해줘".
- `가로쌍막대차트()`·`레이더차트()`의 범례 텍스트에서 업종명("제조업")과 표본 수("7개사 평균,")를 빼고 출처만 남김. 새 헬퍼 `출처_축약()`(정규식으로 "N개사 평균, " 접두어 제거, "샘플값"은 그대로) 추가 — 두 함수가 재사용
- 카드 상단 부제(`#diag-hint`: "제조업 평균 대비 · 7개사 평균, DART 코스닥·코넥스 실제 데이터")와 개요 화면 업종 대비 문장은 표본 수 정보가 유용해 그대로 유지 — 범례에서만 축약
- 검증: `python3 tools/test-e2e.py` (file://) 67개 전부 통과, 실제 렌더링 텍스트 "업종 평균 (DART 코스닥·코넥스 실제 데이터)" 확인, 배포 66개 통과

## [2026-09-03] ERP CSV → GPT 자동 재무데이터 인식 + 절세전략 설명 개선 + 범례 축약

### 1) ERP CSV 업로드 → GPT로 재무데이터 자동 채우기 (Task 27)
사용자 요청: "샘플 데이터 불러오기 대신, ERP CSV를 첨부하면 계정과목 순서가 달라도 GPT API로 매출액·영업이익·인건비·유형자산·감가상각비·이자비용을 자동으로 채워달라".
- **`api/parse-financials.js`** 신규(Vercel 서버리스, 외부 패키지 없음): CSV 원문을 받아 OpenAI Chat Completions API(`gpt-4o-mini`, **Structured Outputs**로 JSON 스키마 강제)에 전달. 시스템 프롬프트가 "계정과목 순서·명칭이 회사마다 다름"을 명시하고 6개 항목 각각의 값(최대 3개년, 오래된→최신 순)과 원래 표시 단위(원/천원/백만원/억원)를 함께 반환하도록 요구
- 인증키는 서버 환경변수 `OPENAI_API_KEY` 사용(BIZINFO/DART와 동일한 패턴). **2026-09-03 기준 미등록** — 등록 전까지는 "GPT API 키가 서버에 등록되지 않았습니다" 오류로 우아하게 대체됨(실제 배포 주소에서 확인됨)
- `index.html`: 재무데이터 표 위에 "ERP CSV로 자동 채우기" UI(파일 선택 + 확인 버튼) 신규 추가(기존 "직접 입력/자동 입력(샘플)" 세그먼트는 그대로 유지 — 삭제하지 않음). `ERP_CSV_불러오기()`가 CSV를 서버로 보내고, 응답의 단위를 백만원으로 환산(원÷100만, 천원÷1000, 억원×100)해 재무데이터 배열([연도-2,연도-1,연도])의 뒤쪽(최신=기준연도)부터 채움. 값을 못 찾은 항목·단위가 불명확한 항목은 상태 문구에 명시
- `vercel.json`에 `api/parse-financials.js` `maxDuration: 30`(GPT 응답 대기용, 다른 함수보다 김) 추가
- 보안·비용: file://(더블클릭)는 서버가 없어 항상 "vercel dev 또는 배포 주소에서만 동작" 안내로 막힘. CSV 내용은 이 서버에 저장하지 않고 로그에도 남기지 않음. **CSV(회사 재무정보)가 처리를 위해 OpenAI로 전송된다는 사실을 화면 안내 문구와 README에 명시**

### 2) 절세전략 — 적용 불가 사유를 정량적으로 설명 (Task 26)
사용자 요청: "적용이 불가능한 항목은 어느 부분이 부족하거나 초과되는지 설명해줘".
- 통합고용세액공제: "지금보다 N명 부족합니다" (증감이 1 미만일 때 필요 인원까지의 차이를 계산)
- 통합투자세액공제: "설비투자 계획액이 0원입니다 — 1원이라도 투자 계획을 입력해야 적용됩니다"
- 근로소득증대세제: 임금 인상률이 기준 증가율보다 몇 %p 낮은지 정확히 표시("계획 X%가 최근 평균 증가율 Y%보다 Z% 낮습니다 — 최소 Y%까지는 올려야 합니다"), 인원 감소·인건비 0원 케이스도 별도 안내 추가

### 3) 절세전략 표에서 법령 근거 문구 숨김 (Task 26)
사용자 요청: "통합고용세액공제 등 밑에 조세특례제한법이 있는데 보이지 않게 해줘" — 전략명 아래 `<small>${근거}</small>` 표시만 제거(데이터 필드 자체는 유지, "자세히 보기" 확장 영역에는 영향 없음).

### 4) 업종 평균 비교 진단 범례 축약 (Task 25)
"업종 평균 (제조업, 7개사 평균, DART 코스닥·코넥스 실제 데이터)" → **"업종 평균 (DART 코스닥·코넥스 실제 데이터)"**로 단축. 신규 헬퍼 `출처_축약()`이 "N개사 평균, " 접두어를 제거(정규식), 카드 상단 부제는 표본 수 정보가 유용해 그대로 유지.

### 검증
- 자기검사(Node): ERP CSV 단위환산·3개년 매핑 로직 4개 케이스 전부 통과, `api/parse-financials.js` 메서드·키 미등록 처리 자기검사 통과
- `python3 tools/test-e2e.py` (file://) **72개 전부 통과** — 신규 검사(근거 안 보임, 적용 불가 부족분 설명, ERP CSV 파일 미선택/file:// 안내) 포함
- `TEST_URL=https://taxnavi-dashboard.vercel.app` **71개 전부 통과** — 실제 배포 서버에서 "GPT API 키가 서버에 등록되지 않았습니다" 오류 문구까지 확인
- 스크린샷으로 절세전략(근거 숨김·부족분 설명)·ERP CSV UI 화면 확인
- `vercel --prod` 재배포 완료

### 남은 과제
- Task 27: `OPENAI_API_KEY` 미등록 — 등록(Vercel 대시보드 또는 별도 터미널, 키 값은 채팅에 붙여넣지 않기) 후 실제 ERP CSV로 재검증 필요

## [2026-09-03] 실시간 시뮬레이션 위젯 추가 + 업종 진단 레이더 크기 축소

사용자 요청: "회사 데이터를 넣었을 때 어떤 곳을 건드려야 세금이 조절 가능한지 직관적으로 알기 어렵다 — 슬라이더나 숫자 입력으로 그래프가 움직이며 한눈에 보이게 해달라. 변하는 항목은 매출 성장률·매출원가율·인건비 증감률. 현재와 기본 시나리오만 보이게. 레이더 차트 크기를 업종 평균 비교 진단 막대 그래프와 같게 줄이고 그 아래에 시뮬레이션을 놓아달라".

### 구현
- **업종 평균 비교 진단 카드 바로 아래**에 "실시간 시뮬레이션" 카드 신규 추가
- 3개 조절 항목(슬라이더 + 숫자 입력 동기화, 드래그·타이핑 모두 가능): 매출 성장률(-30~+30%), 매출원가율(0~100%, 인건비·감가상각비 제외 비용의 매출 대비 비율), 인건비 증감률(-30~+30%). 감가상각비는 고정 유지
- `계산_시뮬레이션()` 신규: 저장된 예측 결과(`계산_시나리오`)는 전혀 건드리지 않고 별도로 그 자리에서만 계산 — 기존 KPI·시나리오표·CSV·절세전략 계산에 영향 없음
- 슬라이더 기본값은 기준연도 실적 비율(매출원가율 자동 계산)로 초기화되어, 조작 전에는 기존 "예측" 결과와 거의 같은 값에서 출발 — "새 분석 시작" 때마다 다시 초기화(값 자체는 저장하지 않는 의도적 단순화, ponytail 주석으로 명시)
- `input` 이벤트로 매 조작마다 즉시 재계산 → KPI 4개(조정 매출·영업이익·세부담·부가세) 숫자와 매출·영업이익 막대 차트 2개(**실적 vs 조정 예측** 2계열만, 기존 세로막대차트 함수 재사용)가 실시간으로 움직임
- 업종 진단 레이더 차트: viewBox 320×320→280×280(R 108→82)로 축소 + `.chart--radar-sm` CSS로 최대 너비 280px 고정(그리드 칸 너비만큼 늘어나지 않게) → 옆의 지표 막대 그래프와 시각적 크기가 비슷해짐

### 검증
- Node 자체검사: 시뮬레이션 계산 로직 3케이스(기본값이 기존 예측과 거의 일치, 매출+10%→정확히 5610, 인건비+20%→영업이익 정확히 266백만원 감소) 통과
- `python3 tools/test-e2e.py` (file://) **75개 전부 통과** — 신규 검사(슬라이더 조작 시 KPI 변경, 슬라이더·숫자칸 동기화, 차트 라벨) 포함, 기존 SVG 개수 검사도 7개로 갱신
- 스크린샷으로 초기 상태·조작 후 상태(매출 +15%·원가율 55%·인건비 -10% → 매출 58.7억원/영업이익 12.2억원으로 즉시 반영) 확인, 레이더-막대 크기 비교 확인
- 배포(`https://taxnavi-dashboard.vercel.app`) 74개 통과

## [2026-09-03] ERP CSV 진입점 이동 + 완료 점 삭제 + 지원사업 공고 요약 삭제

사용자 요청 3건: (1) 재무데이터 카드 안 "ERP CSV로 자동 채우기" 없애고 "분석 시작" 아래 "샘플 데이터 불러오기" 버튼으로 ERP CSV를 넣을 수 있게, (2) 입력·분석결과·절세전략 옆 주황색 완료 점 삭제, (3) 지원사업 공고 목록에 공고명만 보이고 긴 요약·대상 문장은 안 보이게.

### 변경 내용
- 재무데이터 카드 안의 별도 "ERP CSV로 자동 채우기" 행(파일 선택+버튼) 삭제. 대신 사이드바 "분석 시작" 아래 버튼을 "샘플 데이터 불러오기" → **"ERP CSV 불러오기"**로 라벨 변경, 클릭 시 숨겨진 파일 입력을 열어 CSV를 고르면 즉시 GPT 자동 인식이 실행됨(change 이벤트 트리거, 별도 확인 버튼 없음). 데모용 가상 데이터 채우기는 재무데이터 표 위 "자동 입력(샘플)" 세그먼트로 그대로 유지(기능 손실 없음, 진입점만 이동)
- 사이드바 완료 점(`.nav__dot`, 모바일 `.is-done::after`) 관련 CSS·HTML·JS 전부 삭제 — 입력·분석결과·절세전략에서만 표시되던 기능이라 완전히 제거해도 다른 항목에 영향 없음
- `공고표()`에서 요약(`g.요약`)·대상(`g.대상`) 표시 부분 삭제 — 공고 셀에 제목(링크)만 남음. 특히 `g.요약`은 ERP 원문 그대로의 raw HTML 태그(`<p>`, `<div style="...">`)가 이스케이프되어 그대로 텍스트로 노출되던 문제가 있었는데, 아예 안 보이게 되어 함께 해결됨

### 검증
- `python3 tools/test-e2e.py` (file://) **73개 전부 통과** — 샘플 데이터 로딩 테스트를 "자동 입력(샘플)" 세그먼트 클릭으로 변경, ERP CSV 테스트는 `set_input_files`로 change 이벤트 직접 확인(네이티브 파일 다이얼로그는 자동화 불가), 완료 점 검사 삭제
- 스크린샷으로 사이드바(점 없음)·재무데이터 표(ERP 행 없음)·ERP CSV 버튼(새 라벨)·지원사업(공고명만) 확인
- 배포(`https://taxnavi-dashboard.vercel.app`) 72개 통과

## [2026-09-03] 절세전략 출처 정리 + 진단 카드 문구/정렬 + 시뮬레이션 확장(정적 요소 삭제, 표 실시간화)

### 1) 절세전략 참고자료를 공식 사이트로 교체 (Task 30)
사용자 요청: "자세히 클릭하면 참고 자료가 뜨는데 누르면 법무법인이 뜨던데 공인되거나 정부사이트나 국세청 사이트로만 바꿔줘".
- 웹 검색으로 확인한 국세청 공식 페이지(`nts.go.kr` "법인세 공제감면 컨설팅 제도 - 제도 안내", 통합투자세액공제·통합고용세액공제를 명시적으로 다룸)로 `docs/tax-rules.json`의 통합고용세액공제·통합투자세액공제·근로소득증대세제 `출처[1]`을 일괄 교체(기존: nexttala.com·taxguide.im·lawtimes.co.kr 등 사설 블로그·법률신문). `출처[0]`(법제처 법령 원문)은 이미 공식 사이트라 유지
- 절세전략 화면 링크 라벨도 "참고 자료" → "국세청 안내"로 명확히 변경

### 2) 업종 평균 비교 진단 — 문구·정렬 정리 (Task 30)
- 카드 상단 힌트(`#diag-hint`): "제조업 평균 대비 · 7개사 평균, DART 코스닥·코넥스 실제 데이터" → **"제조업 평균 대비 · DART 코스닥·코넥스 실제 데이터"**(기존 차트 범례에만 적용했던 `출처_축약()`을 이 힌트에도 적용)
- 레이더 차트 아래 범례(우리 회사/업종 평균 박스)를 `.chart--radar-sm .chart__legend { justify-content: center }`로 레이더 표 아래 가운데 정렬(다른 차트 범례는 영향 없음, 선택자가 레이더 전용 클래스에 한정)

### 3) 분석결과 상단 고정 설명 문구 삭제 (Task 30)
"샘플정밀(주) · 기준연도 2025년 실적을 바탕으로 2026년을 예측했습니다…" 문구를 기본적으로 숨김. 데이터 이상(비용구조경고: 영업이익+인건비+감가상각비가 매출보다 큰 경우)이 있을 때만 그 경고 문구가 나타나도록 조건부 표시로 변경.

### 4) 실시간 시뮬레이션 확장 — 정적 KPI/차트 삭제, "실적 대비 예측" 표 실시간화 (Task 31)
사용자 요청: "시뮬레이션 바로 아래 있는 예상 매출/영업이익/법인세/부가세 KPI와 매출액·영업이익 비교 그래프를 없애고, 대신 '실적 대비 예측' 표 숫자가 스크롤 값에 따라 실시간으로 바뀌게 해달라".
- `#analysis-kpis`(정적 KPI 4개)와 "매출액 비교"/"영업이익 비교" 정적 차트 카드 삭제 — 실시간 시뮬레이션 카드가 완전히 대체(중복 제거)
- `렌더_시나리오표()`("OOOO년 실적 대비 예측" 표)의 "예측" 열 데이터 소스를 저장된 고정 예측(`분.시나리오[0]`)에서 **현재 시뮬레이션 슬라이더 값**(`계산_시뮬레이션(시뮬상태...)`)으로 변경 — 부제에 "매출 X% · 원가율 Y% · 인건비 Z%"로 현재 슬라이더 상태 표시. `시뮬레이션_다시그리기()`가 슬라이더 조작마다 이 표도 함께 다시 그림
- 절세전략 계산(`계산_절세전략`)·CSV 내보내기·개요 화면 KPI는 그대로 저장된 고정 예측(`상태.분석결과.시나리오[0]`)을 사용 — 시뮬레이션은 이 화면에서만 보이는 "가상 실험"이라 다른 계산에 영향 없음(기존 설계 유지)

### 검증
- `python3 tools/test-e2e.py` (file://) **75개 전부 통과** — 신규 검사(정적 KPI 삭제 확인, SVG 5개로 감소, 슬라이더 조작 시 "실적 대비 예측" 표도 56.1억원으로 갱신·원상복구 시 51억원으로 복귀) 포함
- 절세전략 출처 링크 실제 클릭 경로 확인(`law.go.kr` 법령 원문 + `nts.go.kr` 국세청 안내)
- 스크린샷으로 진단 카드(힌트 축약·레이더 가운데 정렬)·분석결과 화면(정적 요소 삭제, 시뮬레이션 카드 직후 실시간 예측표) 확인
- 배포(`https://taxnavi-dashboard.vercel.app`) 74개 통과

## [2026-09-03] 입력 화면 도움말 문구 축약

사용자 요청: 사이드바 "입력값 지우기" 아래 도움말 문구를 짧게 바꿔달라(vercel dev·배포 주소 안내, 데모 데이터 안내 문장 삭제, "GPT" → "AI"로 순화).
- `python3 tools/test-e2e.py` (file://) 75개 전부 통과, 배포(`https://taxnavi-dashboard.vercel.app`) 74개 통과

## [2026-09-04] 절세전략 2개 추가 — 두루누리 사회보험료 지원 · 청년일자리도약장려금 (Task 32)

사용자 요청: "절세전략에 통합고용세액공제·통합투자세액공제·근로소득증대세제 3개뿐인데 국세청, 국민연금공단, 노동청, 정부 사이트에서 정보를 찾아 더 채우자".

### 조사 결과
웹 검색으로 3개 제도를 확인, 그중 2개를 이번에 추가:
- **두루누리 사회보험료 지원**(국민연금법 시행령·고용보험법 시행령, 국민연금공단·근로복지공단 공동 운영): 상시근로자 10명 미만 사업장이 월평균보수 270만원 미만 근로자를 4대보험에 신규 가입시키면 사업주·근로자 보험료 80% 지원(국민연금 사업주분 월 상한 87,400원, 최대 36개월)
- **청년일자리도약장려금**(청년고용촉진 특별법, 고용노동부·고용24 운영): 취업애로청년(만 15~34세, 월급 450만원 이하) 정규직 채용 후 6개월 이상 고용 유지 시 비수도권 기업 1인당 연 최대 720만원 지원
- **중소기업특별세액감면**(조특법 §7, 국세청)은 조사했으나 **이번 범위에서 제외** — 업종(18개 분류)×지역(수도권/비수도권)×기업 규모(소기업/중기업)에 따라 감면율이 5~30%로 갈리는 매트릭스라 지금의 단순 추정 모델(단일 조건→단일 금액)로는 정확히 반영하기 어려움. 필요하면 별도 작업으로 추가 가능

### 변경 내용
- `docs/tax-rules.json`에 `두루누리사회보험료지원`·`청년일자리도약장려금` 키 추가(근거·출처·요약·조건 수치·주무기관·주의문구 포함, 기존 3개 항목과 동일한 스키마) → `node tools/inline-data.js`로 `index.html` 인라인 데이터 동기화
- `계산_절세전략()`에 두 전략 계산 블록 추가(기존 3개 블록과 동일한 객체 형태 재사용, `렌더_절세()`는 배열 길이에 무관하게 동작해 렌더링 코드 변경 없음)
  - 두루누리: `상시근로자수 < 10` AND 내년 신규 채용(`상시근로자증감 > 0`) 조건, 절감액 = 신규채용 인원 × 국민연금 사업주분 연 지원 상한
  - 청년일자리도약장려금: 내년 청년등 채용 계획(`청년등증가 > 0`) 조건, 절감액 = 채용 인원 × 비수도권 기업 연 지원 상한(720만원)
  - 샘플 데이터(상시근로자수 28명) 기준으로는 두루누리는 "적용 불가"(10명 미만 조건 미충족), 청년일자리도약장려금은 "적용 가능"으로 정상 표시됨 — 조건 분기가 실제로 동작함을 확인
- 절세전략 상세보기(`자세히`)의 "출처" 링크 라벨을 하드코딩된 "법령 원문(법제처)/국세청 안내" 2종에서 URL 도메인 기반 매핑 함수 `출처_기관명()`으로 교체(law.go.kr/nts.go.kr/bokjiro.go.kr/nps.or.kr/moel.go.kr/work24.go.kr 인식) — 새 전략의 출처(복지로·국민연금공단·고용노동부·고용24)가 기존 라벨로 잘못 표시되던 문제를 발견해 함께 수정

### 검증
- `node -e` 구문 검사(HTML 내 인라인 스크립트 블록) 통과
- `python3 tools/test-e2e.py` (file://) **77개 전부 통과** — 신규 검사 추가(전략 5개, 두루누리 적용 불가 문구, 청년일자리도약장려금 적용 가능, 요약 4/5) 및 기존 개요 KPI 검사 값 갱신(3/3 → 4/5)
- 스크린샷으로 절세전략 표 5행 렌더링, 두루누리·청년일자리도약장려금 상세보기(적용 조건·계산 근거·주의·출처 링크) 확인 — 출처 라벨이 "복지로 안내 · 국민연금공단", "고용노동부 공지 · 고용24(work24)"로 정확히 표시됨
- 배포(`https://taxnavi-dashboard.vercel.app`) 76개 통과(1건 차이는 file:// 전용 안내 검사가 서버 환경에서 스킵되는 기존 동작, 실패 0개)

## [2026-09-04] 분기표 하단 문구에서 예정고지 예외 설명 삭제

사용자 요청: "분기별 예상 실적과 세금 일정 아래 '직전 과세기간 공급가액 1억 5천만원 미만 소규모 법인은 예정신고 대신 예정고지 대상' 이 부분 없애자".
- `#quarter-note` 렌더링에서 `데이터.세무규칙.부가가치세.참고` 문구 연결 삭제, 계절성 미반영 안내 문장만 남김. `docs/tax-rules.json`의 해당 필드 자체는 법령 근거 문서로 유지(다른 곳에서 참조하지 않음)
- `python3 tools/test-e2e.py` (file://) 77개, 배포 76개 전부 통과

(참고: 사용자가 KOSIS 국세통계 OpenAPI를 업종 평균 데이터 소스 후보로 조사해 공유함 — 절세전략 3종의 실제 신고 현황 통계(8.3.19/20, 8.3.16)를 발견해 유용하지만, 인건비비율 등 일부 지표는 국세통계에 없고 API 연동은 서버 프록시가 필요해 지금 당장 진행할 작업은 아님. 향후 과제로만 기록.)

## [2026-09-04] 사업자 유형(법인/개인 일반과세자/간이과세자) 추가 + 레이더 차트 방향 오류 수정 (Task 33)

사용자 요청: "입력하는 부분을 누르면 기업 정보를 입력할 수 있잖아 개인사업자인지 간이사업자인지 법인사업자인지 선택할 수 있게 하고... 간이과세자와 개인사업자라면 부가세만 계산을 해주고 법인사업자라면 부가세와 법인세를 보이게 전체 항목이 연동되게 수정하자. ... 그리고 강세 약세 레이더 표 부분이... 정확히 표시가 안된거 같아 점검해".

### 1) 사업자 유형 선택 및 전체 연동
- 기업정보 카드에 "사업자 유형" 선택(법인사업자 / 개인사업자(일반과세자) / 개인사업자(간이과세자)) 추가. `상태.기업정보.사업자유형`(기본값 `corporate`)으로 저장, CSV 내보내기에도 포함
- WebSearch로 2026년 최신 세법 확인 후 `docs/tax-rules.json`에 `간이과세자` 규칙 신규 추가: 기준금액(공급대가 1억 400만원 미만, 2024.7.1. 상향), 납부면제 기준(공급대가 4,800만원 미만), 매입세액공제율(0.5%), 업종 6개 대분류별 부가가치율(제조업 20%·도소매업 15%·서비스업/건설업/정보통신업 30%·음식숙박업 15%) — 부가가치세법 §61·§63·§69 근거, 출처는 법제처·국세청
- `계산_부가세(매출, 매입, 업종코드, 사업자유형)` 신설: 법인·개인(일반과세자)는 기존 (매출−매입)×10%, 간이과세자는 공급대가×업종 부가가치율×10%−매입세액공제(0.5%), 공급대가 4,800만원 미만이면 0원(납부의무 면제). `계산_시나리오()`·`계산_시뮬레이션()`·`렌더_시나리오표()`의 중복 인라인 부가세 계산 3곳을 이 함수로 통일
- `계산_법인세_사업자별()` 신설: 법인사업자만 `계산_법인세()` 호출, 개인·간이는 0원 + "법인세 대상 아님" 안내
- "실적 대비 예측" 표·분기표·개요 KPI·절세전략 KPI를 사업자 유형에 따라 조건부 렌더링(법인세·지방소득세·세부담합계·세후영업이익·세율구간 행은 법인사업자만 표시, 개인·간이는 부가세 행만). 간이과세자는 분기별 예정신고 대신 연 1회 확정신고(다음 해 1월 25일) 문구로 분기표 대체
- **버그 수정**: 재무데이터는 백만원 단위로 저장되는데(`STEERING.md` 규칙), 처음 구현 시 간이과세자 납부면제 기준(4,800만원)을 원 단위로 비교해 51억원 매출에도 "납부의무 면제(0원)"로 잘못 계산되던 문제 발견 → 만원→백만원 단위 변환(`/100`)으로 수정. 실제 매출로 재검증: 간이과세자 8,625만원 vs 일반과세자 1.95억원(간이과세가 낮게 나오는 것이 정상)

### 2) 레이더 차트 방향 오류 수정
업종 평균 비교 진단 레이더에서 "강세·약세"를 표시할 때, 이자비용비율·인건비비율처럼 **낮을수록 좋은 지표**도 매출증가율·영업이익률처럼 **값이 클수록 바깥쪽(강점처럼 보이게)** 그려지고 있었음 — 예를 들어 인건비비율이 업종 평균보다 낮아 실제로는 강점인데도 레이더에서는 안쪽(약점)으로 보였음.
- `계산_진단비교()`에 지표별 `역방향` 플래그 추가(이자비용비율·인건비비율 = true)
- `레이더차트()`의 반지름 계산에서 역방향 지표는 `t = 1 - t`로 반전해, **모든 지표에서 바깥쪽 = 항상 강점**이 되도록 통일. 범례 아래 안내 문구("※ 바깥쪽으로 갈수록 강점입니다…") 추가

### 검증
- `python3 tools/test-e2e.py` (file://) **81개 전부 통과** — 신규 검사 4건 추가(간이과세자 법인세 행 없음·부가세 8,625만원, 개인 일반과세자 법인세 행 없음·부가세 1.95억원, 법인사업자 복구 후 법인세 행 재표시)
- Playwright로 간이과세자·개인(일반과세자)·법인사업자 3가지 전환 시 분석결과 화면 스크린샷 확인, 콘솔 오류 0개
- 사업자유형 필드는 다른 입력칸과 동일하게 `#panel-input` 공용 `input` 리스너로 자동 저장되며, "분석 시작"을 다시 눌러야 반영(다른 입력값과 동일한 기존 UX 패턴 유지)

(참고: 조세특례제한법 세액공제 3종·두루누리·청년일자리도약장려금은 개인사업자에게도 적용 가능한 제도라 사업자 유형과 무관하게 그대로 노출. 다만 개인·간이사업자에서는 "법인세 대비 세액공제" KPI 타일을 숨기고 참고용 안내로 대체)

## [2026-09-04] OPENAI_API_KEY 등록 + GPT 스키마 버그 수정 + 분기표 카드 삭제 (Task 27, 34)

사용자가 OpenAI API 키를 제공("지금 채팅에 키를 붙여넣기" 선택) → 화면에 표시·로그 기록 없이 바로 `vercel env add OPENAI_API_KEY`로 Production/Preview/Development 3개 환경에 등록.

### 실제 호출 중 발견한 버그
등록 직후 배포 서버에서 ERP CSV 업로드를 실제로 테스트하니 "GPT API 호출 실패 (Invalid 'response_format.json_schema.name': string does not match pattern...)" 오류 발생. `api/parse-financials.js`가 OpenAI Structured Outputs 스키마 이름을 한글(`'재무항목추출'`)로 지정하고 있었는데, OpenAI API는 `json_schema.name`이 `^[a-zA-Z0-9_-]+$` 패턴(영문·숫자·언더스코어·하이픈)만 허용 — 이전 세션에서 키 없이 코드만 작성했을 때는 실호출을 못 해봐서 발견되지 않았던 버그. `'financial_items'`로 교체해 해결(응답 JSON의 한글 키 6개(매출액·영업이익 등)는 스키마 name과 별개라 그대로 유지, 문제 없음).

### 검증
- `curl`로 배포 서버 `/api/parse-financials`에 실제 CSV 직접 전송 — 계정과목명이 다른 CSV("급여와임금"→인건비, "금융비용"→이자비용)도 정확히 매핑해 6개 항목 모두 원 단위로 정상 반환 확인
- `TEST_URL=https://taxnavi-dashboard.vercel.app python3 tools/test-e2e.py` — ERP CSV 검사가 "키 미등록" 안내에서 실제 "매출액·영업이익 채웠습니다" 성공 메시지로 바뀜, 실패 0개
- SPECS.md Task 27을 `[ ]` → `[x]`로 갱신(실호출 검증 완료)

### 분기별 예상 실적과 세금 일정 카드 삭제
사용자 요청: "분석결과를 누르면 분기별 예상 실적과 세금 일정... 이부분 필요없어 삭제해".
- `#panel-analysis`에서 "분기별 예상 실적과 세금 일정" 카드(제목·표·안내 문구) 전체 삭제, `렌더_분기표()` 함수와 `렌더_분석()`에서의 호출 제거, 패널 부제 "예측 · 세금 · 분기 · 업종 비교"에서 "분기" 삭제
- 개요 화면의 "다가오는 세금 일정"(별도 기능, `다가오는일정()`)은 그대로 유지 — 사용자가 지목한 건 분석결과 화면의 분기별 표뿐이라 범위 밖

### 검증
- `python3 tools/test-e2e.py` (file://) **80개 전부 통과**(분기표 검사 1건 삭제)
- 배포(`https://taxnavi-dashboard.vercel.app`) 79개 통과, 실패 0개
- 스크린샷으로 분석결과 화면에서 카드가 사라지고 레이아웃이 자연스럽게 이어지는 것 확인

## [2026-09-04] 지원사업 지역 연동, 시뮬레이션 원가율 기본값 0%, 결손 경고, 업종비교 카드 통합 (Task 35)

### 1) 지원사업 지역 필터 ↔ 입력 화면 소재지 연동
사용자 요청: "지원산업 부분... 전남광주를 기반으로 만들었기에... 입력 부분에서 입력된 값을 기반으로 변하게 해줘".
- `#panel-input`의 `change` 리스너에 `f-region`(소재지 선택) 분기 추가 — 소재지를 바꿀 때마다 `상태.지원사업설정.지역`을 `소재지_지역필터[소재지]`로 즉시 동기화하고 저장. 지원사업 화면에 진입하면 `렌더_지원사업()`이 이 값을 `#s-region`에 반영(기존 로직 재사용, 새 렌더 코드 불필요)
- 분석 시작 시 1회만 기본값을 채우던 기존 로직(`if (!상태.지원사업설정.지역) ...`)은 그대로 유지(소재지 select를 아예 건드리지 않은 최초 방문 대비 안전장치)

### 2) 실시간 시뮬레이션 매출원가율 기본값 0%로 변경
사용자 요청: "매출원가율 기본값이 55%로 되어있던데 0으로 변경해줘".
- 기존에는 새 분석마다 `기준연도 실적 비율`(예: 62.2%)로 슬라이더를 자동 세팅했으나, 이제 항상 0%로 시작하도록 변경(`렌더_시뮬레이션()`). 도움말 문구도 "기본값은 기준연도 실적 비율입니다" → "매출원가율 기본값은 0%이며, 슬라이더를 움직여 원가 변화를 반영해 보세요"로 수정
- **알아두실 점**: 원가율 0%가 기본이 되면서, 슬라이더를 하나도 안 건드린 "조정 예측" 초기 화면이 더 이상 실제 원가 구조를 반영한 실적과 자동으로 같지 않습니다(매출원가가 전혀 반영되지 않아 영업이익이 실제보다 커 보임). 슬라이더로 실제 원가율을 입력해야 현실적인 예측이 됩니다.

### 3) 결손(적자) 경고 배너
사용자 요청: 시뮬레이션 슬라이더 조정으로 예측 영업이익이 음수가 되면 결손금 소급공제(법인세법 §72) 안내 경고 표시.
- `#sim-loss-warning`(`.note.note--warn`, 오렌지 톤) 요소를 슬라이더 아래에 추가, `시뮬레이션_다시그리기()`에서 `결.영업이익 < 0`이면 표시하고 실제 계산된 결손 금액을 문구에 실시간 반영. 문구는 사용자가 제시한 "⚠️ 이 예측이 지금 결정을 바꿉니다" 원문을 기반으로 하되, 현재 앱에 없는 "보수 시나리오" 표현은 "조정 예측 기준"으로 바꾸고, 참조처는 "다음 화면" 대신 실제 화면명 "절세전략 화면"으로 구체화

### 4) "업종 평균과 비교" 카드를 "업종 평균 비교 진단" 카드로 통합
사용자 요청: 분석결과 하단의 "업종 평균과 비교"(막대 그래프 + 해석 표)를 없애고, 그 해석 표만 맨 위 "업종 평균 비교 진단" 카드 아래로 옮겨 달라.
- 하단 카드(막대 그래프 `#chart-industry` + 표) 전체 삭제, 해석 표(`#industry-comments`)만 상단 "업종 평균 비교 진단" 카드의 레이더 범례 아래로 이동
- `렌더_업종비교()`에서 막대 그래프·`#industry-hint` 렌더링 코드 삭제, 표 렌더링만 유지(계산 로직 `계산_업종비교()`는 그대로 — 감가상각비율을 포함하는 기존 4개 지표 그대로 사용, 상단 카드의 진단 지표(이자비용비율 포함)와는 별개 데이터셋이라 중복 아님)

### 검증
- `python3 tools/test-e2e.py` (file://) **80개 전부 통과** — SVG 개수 검사 5→4개로 갱신(업종비교 막대 삭제 반영)
- Playwright로 3가지 확인: (1) 업종 평균 비교 진단 카드 아래 해석 표가 렌더링됨, (2) 매출원가율을 95%로 올리면 결손 경고가 실제 결손액(예: −13억원)과 함께 표시됨, (3) 입력 화면에서 소재지를 목포시로 바꾸면 지원사업 화면 지역 필터가 "전남"으로 자동 반영됨. 콘솔 오류 0개

## [2026-09-04] 매출원가율 슬라이더 가운데 정렬 + 절세전략 면책문구 확인일 갱신

- 실시간 시뮬레이션 "매출원가율" 슬라이더를 `min="0" max="100"`(기본값 0이 맨 왼쪽) → `min="-100" max="100"`(기본값 0이 정중앙)으로 변경 — 매출 성장률·인건비 증감률 슬라이더와 동일하게 가운데에서부터 좌우로 스크롤 가능. 기본값은 요청대로 0% 그대로 유지
- `docs/tax-rules.json`의 최상위 `확인일`을 2026-09-03 → 2026-09-04로 갱신(절세전략 화면 하단 면책문구에 표시), `node tools/inline-data.js` 동기화
- `python3 tools/test-e2e.py` (file://) 80개, 배포 79개 전부 통과

## [2026-09-04] 업종 평균 비교 해석 표 순서·지표를 진단 차트와 통일

사용자 요청: "업종 평균 비교 진단 아래에 있는 [해석 표]... 매출 증가율, 영업이익률, 이자비용비율, 인건비 비율 순서대로 볼 수 있게 해줘".

이전 해석 표는 `계산_업종비교()`(영업이익률·인건비비율·매출증가율·감가상각비율, DART에 없는 감가상각비율은 항상 "데이터 없음")를 따로 계산해 사용하고 있어, 바로 위 진단 막대·레이더 차트(`계산_진단비교()`: 매출증가율·영업이익률·이자비용비율·인건비비율)와 지표 구성도 순서도 서로 달랐다.
- 표를 `계산_진단비교()`가 이미 만드는 `진단비교.행`(지표 4개가 정확히 요청한 순서로 들어있음)을 그대로 사용하도록 변경 — 별도 계산 불필요
- `업종해석()`에서 더 이상 쓰이지 않는 `감가상각비율` 문구를 `이자비용비율`(매출 대비 이자비용 부담 해석)로 교체
- 표에서 우리 회사 값이 없는 경우("이자비용 미입력") 안내를 추가(`데이터 없음`은 업종 평균이 없을 때, `미입력`은 우리 회사 값이 없을 때로 구분)
- 이제 쓸모없어진 `계산_업종비교()`(감가상각비율 계산 전용)와 `계산_전체()`의 `업종비교` 필드를 삭제, 개요 화면의 "업종 대비" 한 줄 요약도 `진단비교`를 공용으로 사용하도록 통합 — 같은 값을 두 번 계산하던 중복 제거

### 검증
- `python3 tools/test-e2e.py` (file://) 80개, 배포 79개 전부 통과(배포 1차 실행에서 기업마당 API 호출 타이밍으로 인한 일시적 네트워크 오류 1건 있었으나 재실행 시 재현 안 됨 — 코드 변경과 무관)
- Playwright로 해석 표 4행이 매출증가율→영업이익률→이자비용비율→인건비비율 순서로 정확히 렌더링되는 것을 텍스트로 확인

## [2026-09-04] 지원사업 지역 27개 세분화 + 시뮬레이션 원가율 기준점 수정 + 전체 점검 (Task 36)

### 1) 지원사업 지역 필터를 27개 구·시·군으로 세분화
사용자 요청: "지원사업 부분을 보면 지역을 누르면 전남광주에 해당되는 지역만 고를 수 있게 해줘"(AskUserQuestion에서 "27개 구·시·군으로 세분화" 선택).
- `#s-region`을 기존 4개(전체/광주+전국/전남+전국/전국공통만) → **전체 + 27개 구·시·군(입력 화면 소재지와 동일한 optgroup 구조) + 전국 공통만 = 29개**로 확장. 값 체계도 소재지 코드(`gwangsan`, `mokpo` …)로 통일
- 공고 데이터 자체는 광주/전남/전국 3버킷뿐이라(기업마당 API 응답도 `공고정규화()`에서 3버킷으로 정규화됨), `필터된공고목록()`에서 선택한 구·시·군 코드를 `소재지_지역필터`로 버킷에 매핑한 뒤 비교 — 같은 도 안에서는 결과가 동일하다는 점을 사용자에게 미리 안내하고 진행
- 소재지 ↔ 지역 필터가 같은 코드 체계가 되면서 동기화 로직이 단순해짐(`소재지_지역필터[...]` 매핑 제거, 값 그대로 대입). `#support-count` 라벨은 `소재지이름[]`으로 한글 지역명 표시
- LocalStorage에 남아 있는 옛 값('광주'·'전남')은 `불러오기_지원사업설정()`에서 소재지 코드로 자동 이전(select에 없는 값이 들어가 빈 선택으로 보이는 문제 방지)

### 2) 매출원가율 슬라이더를 "기준연도 대비 증감(%p)"으로 변경 — 계산 오류 수정
직전 작업에서 원가율 기본값을 0%로 바꾼 뒤 점검해 보니, **슬라이더를 건드리지 않은 기본 예측이 실적과 크게 어긋나는 계산 문제**가 확인됐다(샘플 데이터 기준 영업이익 3.8억원 → 35.5억원으로 9.3배 과대, 세부담 6,160만원 → 7.59억원). 원가율을 절대 비율로 두고 0%를 기본값으로 쓰면 매출원가가 아예 빠지기 때문.
- `계산_시뮬레이션()`의 원가율 인자를 **절대 비율 → 기준연도 실적 대비 증감(%p)**으로 변경. `기준원가율()`(= (매출−영업이익−인건비−감가상각비)/매출)을 기준점으로 삼고 `적용원가율 = max(0, 기준원가율 + 증감)`으로 계산 — 세 슬라이더 모두 "기준연도 대비 증감"으로 의미가 통일되고, 전부 0이면 예측이 실적과 정확히 일치(검증: 영업이익 380백만원, 법인세 56,000,000원, 부가세 195,000,000원 모두 실적과 동일)
- 슬라이더 범위도 다른 두 개와 같은 `-30~30`, 기본값 0이 정중앙. 라벨을 "매출원가율 증감"으로 바꾸고 `(기준연도 62.2% → 적용 72.2%)`처럼 실제 적용 비율을 실시간 표시해 절대값도 확인 가능
- "실적 대비 예측" 표 부제도 `원가율 +0%p` 형식으로 변경

### 3) 전체 점검에서 찾아 고친 것
사용자 요청: "전체적으로 오류 이상이나 계산에 문제가 있으면 수정하면서 점검해".
- **막대차트 음수 라벨 겹침(그래프 이상)**: `세로막대차트()`에서 음수 막대의 값 라벨을 막대 끝 아래(`top + h + 14`)에 그려서, 결손이 큰 경우 x축 항목명("조정 예측 2026년")과 겹쳐 읽을 수 없었음 → 0선 위 빈 공간(`y0 - 8`)으로 이동. 결손 시뮬레이션에서 "−8.95억원"이 겹치지 않고 정상 표시되는 것 확인
- **절세전략 카드 힌트 문구**: "규칙: docs/tax-rules.json · 원 단위" → "금액 단위: 원"(내부 파일 경로 노출 제거, 사용자 요청)
- `가로쌍막대차트()`의 미사용 변수(`const x = v => ...`) 삭제

### 계산 검증(독립 재계산과 대조 — 모두 일치)
| 항목 | 앱 | 수동 계산 |
|---|---|---|
| 법인세(과표 380백만) | 56,000,000원 | 20 + (380−200)×20% = 56백만 ✓ |
| 지방소득세 | 5,600,000원 | 56×10% ✓ |
| 부가세(일반) | 195,000,000원 | (5,100−3,150)×10% ✓ |
| 부가세(간이·제조 20%) | 86,250,000원 | 5,100×20%×10% − 3,150×0.5% ✓ |
| 통합고용(1년차/3년) | 17,000,000 / 81,000,000원 | 청년1,000+그외700만 / 4,900+3,200만 ✓ |
| 통합투자 | 41,250,000원 | 400×10% + max(0,400−387.5)×10% ✓ |
| 근로소득증대 | 1,218,079원 | 1,330×(8%−7.5421%)×20% = 1,218,078.5 ✓ |
| 진단 4지표 | 9.7 / 7.5 / 0.4 / 26.1% | 모두 일치 ✓ |

### 엣지 케이스 점검(콘솔 오류 0개, 모두 정상)
영업이익 적자(−500), 매입액 미입력(매출세액만 5.1억원), 이자비용 미입력(진단 표 "미입력" 표시), 매출 0, 간이과세자+적자, 소재지 신안군(지역 필터 자동 전환·8건)

### 검증
- `python3 tools/test-e2e.py` (file://) **80개 전부 통과**(지역 필터 기본값 검사를 `gwangsan`으로 갱신), 배포 79개 통과

## [2026-09-04] 진단 레이더 차트 삭제 + 동종 상장기업 비교군 분석 도구 신규 (Task 37, 19-P)

### 1) 업종 평균 비교 진단에서 레이더(강세·약세) 차트 삭제
사용자 요청: "강세 약세 레이더 표에 표시되고 있는데 이는 지우고".
- `#chart-diag-radar` div, `레이더차트()` 함수(약 40줄), `.chart--radar-sm` CSS, `렌더_진단비교()`의 호출부를 모두 삭제. 레이더 전용으로만 쓰이던 `계산_진단비교()`의 `역방향` 플래그도 함께 제거(죽은 코드)
- 남은 가로 막대 차트를 `chart--wide`로 카드 전체 너비 사용. 진단 카드는 이제 "지표 막대 + 해석 표" 구성
- `tools/test-e2e.py`: SVG 개수 검사 4→3개, 지표 4개 검사 대상을 레이더→막대 차트로 변경. **80개 전부 통과**

### 2) tools/peer-cluster — 동종 상장기업 비교군 분석 CLI (신규)
STEERING.md의 "외부 라이브러리 금지"는 `index.html`에만 적용된다는 사용자 지침에 따라, 이 도구는 pandas·scikit-learn·requests·openai·python-dotenv·matplotlib를 사용(`requirements.txt`로 관리). **택스네비 본체(index.html, docs/*.json)는 전혀 수정하지 않는 독립 실행형**이며, 결과를 화면에 연동하는 것은 SPECS.md 향후 과제로만 기록했다.

파일 구성: `run.py`(CLI) · `dart_client.py`(호출 공통부) · `peers.py`(비교군) · `financials.py`(재무) · `features.py`(특성) · `cluster.py`(K-means) · `gpt_label.py`(군 이름) · `growth.py`(성장 판정) · `disclosures.py`(공시) · `report.py`(리포트) + `tests/`(pytest 25개) + `README.md` + `sample_target.json`

주요 설계:
- **키 보호**: DART·OpenAI 키는 `.env`에서만 읽고 코드·로그·리포트·캐시 파일명 어디에도 남기지 않음. `.gitignore`에 `.env`·`cache/`·산출 CSV·`reports/` 추가
- **호출 절약**: 엔드포인트별 캐시(고유번호 30일 / 기업개황 30일 / 재무 1일 / 공시 1시간), 재시도 3회 지수 백오프, 호출 간 0.2초, 일일 20,000건 카운터(90% 도달 시 경고)
- **비교군 확장 로직**: 업종코드 앞자리 L=min(4, 코드길이)부터 비교해 15곳 미만이면 L을 3→2로 줄이고, 2자리에서도 부족하면 진행하되 리포트에 경고
- **역할 분리**: 군 소속은 K-means(k=3, random_state=42)가 결정하고, GPT는 A/B/C 라벨·이름·기준 서술만 작성(temperature 0, JSON 모드). GPT 실패 시 군번호 순 A/B/C로 대체하고 리포트에 "GPT 라벨 실패" 표시. 프롬프트·응답 원문은 `cache/gpt_log/`에 저장
- **--dry-run**: `tests/fixtures`의 가짜 회사 20곳×5개년으로 API 키 없이 전체 파이프라인 실행(실습·시연용). 픽스처는 고성장 소형/안정 중견/정체 소형 3덩어리로 설계해 K-means가 실제로 3군을 찾아내는지 검증됨(실루엣 0.831)

만들면서 찾아 고친 버그 2건:
- **단위 불일치(중요)**: 기준 기업 입력은 백만원인데 DART 비교군 금액은 원이라, `log10(매출액)` 특성이 3.71 vs 10.53으로 어긋나 군 배정이 왜곡됐다(중심까지 거리 17~19). `기준기업특성()`에서 백만원→원 환산을 추가해 9.71 vs 10.83으로 정상화(거리 3.2~4.6), 배정 결과도 감소군 → 성장군으로 바로잡힘
- **백분위 표기 오류**: 군 안에서 1등인데 "상위 0%"로 표시되던 문제 → "8곳 중 8위(상위 100%)"처럼 순위 기반 표기로 변경

### 검증
- `python3 tools/peer-cluster/run.py --target tools/peer-cluster/sample_target.json --dry-run` — 오류 없이 리포트(.md) + 상자그림(.png) + CSV 5종 생성
- `pytest tools/peer-cluster/tests` — **25개 전부 통과**(특성 계산, prefix 확장, K-means 배정, GPT JSON 실패 대체, 판정 경계값)
- 리포트 구조 확인: 1 개요 → 2 비교군 표 → **3 분류 기준 표(비교군 표 바로 아래)** → 4 기준 기업 판정 → 5 성장 분석 → 6 공시 → 7 방법론·한계·면책(API 호출 수 포함)

## [2026-09-04] 시뮬레이션 조작 항목 교체 · CSV 내보내기 삭제 · 인쇄 범위 축소 (Task 38)

### 1) 실시간 시뮬레이션: 매출원가율·인건비 → 영업이익률·이자비용 비율
사용자 요청: "매출원가율 증감이랑 인건비 증감률을 지우고, 영업이익률이랑 이자비용 비율을 스크롤해서 변하게 해줘".
- 슬라이더 구성: **매출 성장률(기준연도 대비 증감) · 영업이익률(매출 대비 비율) · 이자비용 비율(매출 대비 비율)**. 뒤 두 개는 기본값이 기준연도 실적 비율이라 그대로 두면 예측 = 실적(step 0.01로 반올림 오차 0.02% 이내)
- 계산 모델 변경: 영업이익 = 매출 × 영업이익률, 이자비용 = 매출 × 이자비용비율. **법인세 과세표준을 영업이익 − 이자비용(법인세차감전순이익 근사)으로 변경** — 그러지 않으면 이자비용 슬라이더가 아무 값에도 영향을 주지 않는다
- 이 과세표준 기준을 저장되는 예측(`계산_시나리오`)에도 똑같이 적용해 개요 KPI·절세전략과 분석 화면이 어긋나지 않게 통일. 샘플 기준 세부담이 6,160만원 → **5,720만원**으로 바뀜(과세표준 380 → 360백만원)
- "실적 대비 예측" 표에 **이자비용**·**과세표준(영업이익 − 이자비용)** 행 추가, `계산_시나리오`의 실적 객체에 이자비용 추가

### 2) CSV 내보내기 삭제
사이드바·모바일 버튼, `CSV내보내기()` 함수(약 30줄), `#i-csv` 아이콘, 액션 바인딩까지 모두 제거. 남은 액션은 인쇄/PDF·데이터 초기화 2개

### 3) 인쇄 / PDF 저장 범위를 분석결과 3개 카드로 축소
사용자 요청: "업종 평균 비교 진단이랑 실시간 시뮬레이션 2026년 실적 대비 예측 부분을 인쇄하거나 pdf로 저장".
- 인쇄 CSS를 전면 교체: 모든 화면을 숨기고 `#panel-analysis`만 출력, `#analysis-content` 안에서도 `.print-keep`이 붙은 3개 카드(진단·시뮬레이션·실적 대비 예측)만 남긴다. 사이드바·툴바·하단 탭바·슬라이더 컨트롤(`#sim-controls`)·결손 경고는 인쇄에서 제외
- 인쇄에서만 보이는 머리글(`#print-header`) 추가: "택스네비 분석결과 — 회사명 / 기준연도·예측연도·출력 시각". 화면에서는 `.print-only`로 숨김
- 현재 어느 화면을 보고 있든 인쇄하면 분석결과 3개 카드가 나온다(패널의 `hidden` 속성을 인쇄 CSS가 덮어씀)

### 검증
- `python3 tools/test-e2e.py` (file://) **83개 전부 통과** — 인쇄 검사를 "패널 5개 출력" → "분석결과 패널만·카드 3개만·머리글 포함·슬라이더 숨김" 4건으로 교체, 세부담 기대값 5,720만원으로 갱신, CSV 검사 삭제, 슬라이더 id 변경 반영
- 배포(`https://taxnavi-dashboard.vercel.app`) 82개 통과. 인쇄 미리보기 스크린샷으로 3개 카드+머리글+면책문구만 출력되는 것 확인

## [2026-09-04] 마감 숨기기 삭제 · 사업자 유형별 전면 분기 · 개인/간이 절세제도 3종 추가 (Task 39)

### 1) "마감된 공고 숨기기" 기능 삭제
사용자 확인: 마감된 공고는 애초에 가져오지 않아 필터가 무의미. 체크박스·`마감숨김` 상태·필터 로직·바인딩을 모두 제거(저장 키 구조에서도 삭제).

### 2) 사업자 유형이 절세전략·지원사업·세금 일정까지 연동
- **다가오는 세금 일정**(개요): `docs/tax-rules.json`에 `개인_신고일정`(1기 확정 7/25, 2기 확정 다음 해 1/25)·`간이_신고일정`(확정신고 다음 해 1/25)을 추가하고, 유형에 따라 일정표를 갈아끼운다. 법인만 법인세(3월 신고·8월 중간예납)를 포함하고, 개인·간이는 **부가세만** 표시. 부가세 추정액도 신고 횟수로 나눠 회차별로 보여준다(간이는 연 1회 전액)
- **절세전략**: `tax-rules.json` 각 제도에 `대상사업자` 배열을 넣고, `계산_절세전략()`이 현재 유형에 맞는 제도만 목록에 남긴다. 전략명 아래 대상(예: "개인·간이사업자 (법인 제외)")을 표기
- **지원사업**: 공고 데이터에 사업자 유형 필드가 없어, 제목·대상·태그·요약 문구의 키워드로 대상을 추정하는 `공고_대상유형()`을 추가(소상공인·자영업·개인사업자 → 개인 / 법인·벤처기업·중견기업 → 법인 / 단서 없으면 공통). "내 사업자 유형에 맞는 공고만" 체크박스로 거를 수 있고, 목록에는 추정 대상 칩을 표시. **키워드 추정이므로 참고용**이며 공통 공고는 항상 포함

### 3) 개인·간이사업자도 신청 가능한 절세 제도 3종 추가 (기존 5종 → 최대 8종)
법인 대상 위주였던 목록에, 웹 검색으로 확인한 2026년 기준 제도를 추가했다.
- **노란우산공제(소기업·소상공인 공제부금, 조특법 §86의3)** — 사업소득 구간별 소득공제 한도(4천만원 이하 600만원 / 1억원 이하 300만원 / 초과 200만원). **세액공제가 아닌 소득공제**라서 절감액 = 한도 × 한계세율로 계산하기 위해 `종합소득세` 누진세율표(소득세법 §55, 6~45%)와 `한계소득세율()` 함수를 새로 넣었다. 법인은 "법인 대표 조건부"로 표시하되 적용 불가 처리
- **신용카드 등 매출세액공제(부가세법 §46)** — 개인사업자 전용(법인 제외, 간이 포함). 발급금액의 1.3%·연 1,000만원 한도(2026.12.31.까지 한시 특례, 이후 1%·500만원). 카드 매출 비중을 입력받지 않으므로 "매출 전액이 카드·현금영수증"이라 가정한 **최대 추정치**임을 요약·주의에 명시
- **간이과세자 부가세 납부의무 면제(부가세법 §69)** — 간이과세자 전용. 공급대가 4,800만원 미만이면 납부 면제(신고의무는 유지)

검증 결과 유형별 전략 수: **법인 6종 · 개인 일반과세자 7종 · 간이과세자 8종**

### 검증
- `python3 tools/test-e2e.py` (file://) **90개 전부 통과** — 신규 6건(개인: 신용카드공제 포함 7종·간이 전용 제외·법인세 일정 없음 / 간이: 8종·확정신고 1회만 / 지원사업: 마감 체크박스 삭제·유형 필터 동작) 추가, 마감 숨기기 검사 3건 삭제, 전략 수 5→6·요약 4/6으로 갱신
- 배포(`https://taxnavi-dashboard.vercel.app`) 88개 통과

### 참고: peer-cluster 도구 DART 실제 실행 결과(키 사용, 완료 조건 3)
`python3 tools/peer-cluster/run.py --target tools/peer-cluster/sample_target.json` 성공 — 상장사 3,988곳 중 코스닥·코넥스 **1,804곳** 수집, 업종 2929 매칭으로 **비교군 16곳(L=4, 확장 없음)**, 재무 확보 14곳 중 특성 완비 8곳으로 3개 군 생성(실루엣 0.329), 소속 군 공시 269건 수집, 리포트·CSV 정상 생성. GPT 라벨은 로컬에 `OPENAI_API_KEY`가 없어 A/B/C 대체 배정으로 처리됨(키를 `.env`에 넣으면 정상 동작하며, `gpt_label.py`가 `.env.local`도 함께 읽도록 보완). 표본이 작아 군 크기가 6/1/1로 치우친 점은 리포트의 '한계'에 이미 기록되는 구조.

## [2026-09-04] 비교군 최소 인원 기준 수정 + 분석결과에 K-means 시각화·해설 연동 (Task 40)

### 1) "최소 15곳"을 매칭 수 → **실제 분석 가능 회사 수** 기준으로 수정
기존에는 업종코드로 매칭된 회사 수만 보고 확장을 멈춰서, 16곳이 매칭돼도 재무 결측을 빼면 8곳만 남아 군이 6/1/1로 치우쳤다.
- `peers.비교군_후보들()`을 새로 만들어 L=4→3→2 후보를 목록으로 돌려주고, `run.py`가 각 후보마다 **재무 수집 → 특성 계산 → 전처리까지 끝낸 뒤 사용 가능 회사 수**로 판단해 확장을 결정하도록 변경
- 실제 실행 결과: 4자리 16곳(분석 가능 8곳) → **3자리 151곳(분석 가능 131곳)**으로 자동 확장, 군 크기가 28/94/9로 정상화되고 실루엣도 0.329 → 0.358로 개선

### 2) 분석결과 "업종 평균 비교 진단"에 K-means 시각화·비교군 해설 추가
`run.py`가 화면용 요약 `docs/peer-cluster.json`을 함께 저장하고, `tools/inline-data.js`가 `#data-peer-cluster` 블록으로 인라인한다(데이터가 없으면 해당 영역은 자동으로 숨김).
- **막대 차트 바로 옆**: K-means 결과 산점도(가로 = 매출 규모 log, 세로 = 3년 CAGR). 131개 회사를 점으로 찍고 군별로 오렌지 채움/회색 채움/빈 원으로 구분(디자인 토큰 외 색 미사용), 군 중위값은 ✕, 우리 회사는 오렌지 다이아몬드 + "우리 회사" 라벨. 그 아래 **작동 프로세스 4단계**를 글로 설명
- **해석 표 아래**: 군 요약 표(군·이름·기업 수·3년 CAGR 중위·증가 비율·판정, 소속 군 강조) + 우리 회사가 어느 군에 왜 들어갔는지 **문장 해설**(소속·2순위·군 판정·군 내 순위·규모 배수·군 중위값 대비 앞선 지표/뒤진 지표) + 지표 비교 표(읽는 법 포함)
- 한국어 조사 처리 함수(`조사_으로`) 추가 — "감소으로" → "감소로"

샘플 데이터 실제 출력: 샘플정밀(주)는 **A군(저성장 기업군, 28곳)**에 배정되고, 군은 3년 CAGR 중위 −20.6%·증가 비율 10.7%로 "감소" 판정. 다만 우리 회사의 3년 CAGR 10.2%는 군 내 29곳 중 3위(상위 10%), 영업이익률 7.5%(군 중위 −30.3%)로 **군에서 규모는 가장 작지만 성장·수익성은 앞서는** 위치임이 문장으로 설명된다.

### 3) --dry-run이 화면 데이터를 덮어쓰던 문제 수정
`--dry-run`은 가짜 회사 20곳 데이터라 `docs/peer-cluster.json`을 덮어쓰면 화면에 가짜 비교군이 표시된다. dry-run일 때는 `tools/peer-cluster/peer-cluster.dryrun.json`에만 쓰도록 분리(.gitignore 추가).

### 검증
- `python3 tools/test-e2e.py` (file://) **94개 전부 통과** — 신규 4건(산점도 점 15개 이상·프로세스 4단계·군 3개와 소속 해설·우리 회사 표시), SVG 개수 3→4 갱신
- `pytest tools/peer-cluster/tests` 25개 통과, `--dry-run`도 정상
- 배포(`https://taxnavi-dashboard.vercel.app`) 92개 통과

## [2026-09-04] K-means 산점도 세로축 ±50% 고정

사용자 요청(배포 확인 후): "표를 +-50%까지 늘려줘".
- 비교군 산점도의 세로축(3년 매출 CAGR)을 5~95 백분위 자동 범위(기존 약 −35%~+55%) → **−50% ~ +50% 고정**으로 변경, 눈금도 −50/−25/0/25/50%의 25% 간격으로 정리해 0선 기준 위아래 비교가 쉬워졌다.
- 범위를 벗어나는 회사(급성장·급감)는 기존처럼 가장자리에 붙여 표시하되, **몇 곳이 범위 밖인지 안내 문구를 추가**(현재 데이터 기준 10곳)하고 실제 값은 마우스 툴팁으로 확인하도록 했다.
- 가로축(매출 규모 log)은 퍼센트 축이 아니라 그대로 데이터 최소~최대 범위를 사용한다.
- `python3 tools/test-e2e.py` 94개, 배포 92개 전부 통과

## [2026-09-04] K-means 산점도 군 구분을 모양 → 색으로 변경

사용자 요청: "a군은 주황색 b군은 하늘색 c군은 회색으로 보이게 해줘".
- 기존에는 STEERING의 "새 색상 추가 금지" 규칙을 지키려고 오렌지 채움 / 회색 채움 / 빈 원(모양)으로 군을 구분했는데, 요청에 따라 **라벨 기준 고정 색**(A=주황 `--chart-forecast`, B=하늘색, C=회색 `--chart-reference`)으로 바꿨다.
- **하늘색은 디자인 토큰에 없던 색**이라 임의로 정하지 않고 dataviz 검증 스크립트(`validate_palette.js`)로 후보 4개를 색맹 판별 검사한 뒤 가장 안전한 **`#4FB3E8`**을 선택(회색 대비 ΔE 18.1 deutan / 20.5 tritan, 정상시각 19.6 — 모두 기준 통과). `--cluster-a/b/c` 토큰으로 정의하고 "K-means 군 구분 전용, 사용자 요청 예외"라고 주석에 남겼다.
- A군 주황과 우리 회사 마커가 겹쳐 보이지 않도록 우리 회사 다이아몬드를 **진한 오렌지(`--primary-600`) + 흰 테두리 2.5px**로 바꾸고 범례에도 항목을 추가했다. 툴팁에도 소속 군 라벨을 표시.
- `python3 tools/test-e2e.py` 94개, 배포 92개 전부 통과

## [2026-09-04] 업종 비교 표 "해석"을 GPT 실시간 생성으로 교체 (Task 41)

사용자 요청: "해석부분은 gpt api로 gpt ai가 실시간으로 분석해서 해석한 결과를 해석에 각각 작성해줘".
- **`api/interpret-metrics.js` 신규**(Vercel 서버리스). 브라우저가 지표 숫자(우리 회사·업종 평균·차이)만 보내면 서버가 대신 OpenAI를 호출한다. 인증키는 서버 환경변수 `OPENAI_API_KEY`(이미 등록됨)에만 있고 브라우저에 내려가지 않으며, **회사명 등 식별정보는 아예 전송하지 않는다**(업종명·숫자만)
- 프롬프트에 "이자비용비율·인건비비율·감가상각비율은 낮을수록 유리"를 명시해 방향을 반대로 읽지 않게 했고, 없는 숫자 창작·단정적 세무 판단·투자 권유를 금지. Structured Outputs(`metric_interpretations` 스키마, 배열 형태)로 형식 고정, temperature 0.2, 120자 제한
- 화면은 **기본 해석(`업종해석()`)으로 먼저 그린 뒤** 서버 응답이 오면 그 칸만 교체하는 방식이라, `file://`(서버 없음)이나 호출 실패 시에도 기존 문구가 그대로 남는다. 로딩 중에는 "· AI가 분석 중…" 표시, 성공 시 표 위에 AI 생성 안내 문구 노출
- 같은 숫자로 다시 그릴 때는 재호출하지 않도록 지표 값 기준 메모리 캐시 적용(화면 전환마다 API를 부르지 않음)

실제 배포 출력 예: "우리 회사의 이자비용비율이 0.39%로 업종 평균 1.6%보다 1.21%p 낮습니다. 이는 재무 건전성을 나타내므로 안정적인 자금 관리를 유지하세요." — 낮을수록 좋은 지표의 방향을 정확히 해석함

### 작업 중 발견해 고친 것
`렌더_업종비교()`에 GPT 갱신 호출을 넣는 치환이 공백 불일치로 조용히 실패해, API는 정상인데 화면은 그대로인 상태로 한 번 배포됐다. 브라우저 네트워크 로그에 요청 자체가 없는 것을 보고 원인을 찾아 호출부를 다시 삽입하고 재배포. **치환 후 실제 삽입 여부를 grep으로 확인하는 절차를 거쳤어야 했다.**

### 검증
- `curl`로 배포 API 직접 호출 → 지표 4개 해석 정상 수신
- Playwright로 배포 사이트 확인: `/api/interpret-metrics` 200 응답 2건, 표의 해석 4칸이 GPT 문장으로 교체, AI 안내 문구 노출
- `python3 tools/test-e2e.py` (file://) **96개**, 배포 **95개** 전부 통과 — 신규 검사 2건(해석 칸 4개, 서버 모드 GPT 반영/미서버 모드 기본 해석 유지)

## [2026-09-04] 비교군 섹션 하단 출처·면책 문구 삭제

사용자 요청으로 비교군 해설 맨 아래의 "※ 비상장 중소기업인 우리 회사를 상장사와 비교한 참고 자료이며 투자 권유가 아닙니다. 출처: 금융감독원 전자공시시스템(DART) · 생성 YYYY-MM-DD" 한 줄을 삭제했다.
- 화면 하단 공통 면책 문구(`#footer-disclaimer`)와 절세전략 탭 면책(`#tax-disclaimer`)은 STEERING 7항 요구사항이라 그대로 유지된다. DART 출처 표기는 peer-cluster 리포트(.md) 7장 '방법론·한계·면책'에도 남아 있다.
- `python3 tools/test-e2e.py` 96개, 배포 95개 전부 통과

## [2026-09-04] 개요 화면을 한 화면에 꽉 차게 (Task 42)

사용자 요청: "개요 부분 한눈에 볼 수 있게 화면을 꽉 채워". 기존에는 본문 폭이 1400px로 제한되고 카드 높이도 내용만큼만 잡혀, 1600×900 화면에서 아래쪽 45%가 빈 채로 남았다.
- **가로**: 개요 화면일 때만 `.content`의 `max-width: 1400px` 해제(`.content--full`) → 사이드바를 뺀 전체 폭 사용
- **세로**(≥1024px): `.main--full`을 `height:100vh; overflow:hidden`으로 두고 `.content`를 flex 컬럼으로 만들어, 패널이 남는 높이를 채우고 카드 목록이 넘치면 **카드 안에서만 스크롤**되게 했다. 툴바 높이는 화면 폭에 따라 달라져 `calc(100vh - var(--toolbar-h))`가 어긋났기 때문에 flexbox로 자동 계산하도록 바꿨다
- 빈 공간을 실제 정보로 채우기 위해 다가오는 세금 일정·임박 지원사업 표시 건수를 각각 3건 → **5건**으로 확대
- 좁은 화면(<1024px)은 억지로 밀어 넣지 않고 기존처럼 세로 스크롤을 허용

### 작업 중 잡은 문제 2가지
1. `.content--full .panel { display: flex }`가 `[hidden]` 속성을 덮어써서 **숨겨진 화면 4개가 모두 렌더링**됐다(문서 높이 5657px). `:not([hidden])`으로 한정해 해결
2. 높이를 고정하자 면책 문구(`.content__foot`)가 화면 밖으로 잘렸다 — STEERING 7항의 "화면 하단 면책 문구 상시 표시" 위반이라, `.content`를 flex 컬럼으로 만들어 푸터 자리를 확보

### 검증
- 1920×1080 / 1600×900 / 1440×900 / 1280×800 / 1024×768 전부 **세로·가로 스크롤 없이 면책 문구까지 한 화면**에 표시. 768×1024·375×812는 기존대로 스크롤
- `python3 tools/test-e2e.py` (file://) **99개**, 배포 **98개** 전부 통과 — 신규 검사 3건(스크롤 없음, 면책 문구 노출, 본문이 사이드바 뺀 전체 폭 사용) 추가, 세금 일정 건수 검사 3→5로 갱신

## [2026-09-04] 비교군이 업종·재무데이터 변경에 실시간 반응하도록 수정 (Task 43)

사용자 지적(실제 버그): "업종을 선택하면 K-means 클러스터 표에 들어가는 기업들도 실시간으로 변해야 하는데 안 변해".
원인 — 비교군 데이터가 **제조업 업종코드 2929 하나로 미리 만든 고정 스냅샷**이었다. 업종을 바꿔도, 재무데이터를 고쳐도 같은 131곳이 그대로 나왔다.

### 해결: 업종별 사전 계산 + 우리 회사 배정은 브라우저 실시간 계산
1. **`peers.업종별_비교군()` 신규** — 택스네비 6개 업종을 KSIC 10차 2자리 대분류 범위로 매칭한다(제조업 10~34, 건설업 41~42, 도소매업 45~47, 음식·숙박업 55~56, 정보통신업 58~63, 서비스업 70~76·94~96). 앞자리 prefix 하나로는 "제조업 10~34" 같은 넓은 대분류를 표현할 수 없어 범위 목록 방식으로 바꿨다.
2. **`tools/peer-cluster/build_all_industries.py` 신규** — 6개 업종을 한 번에 돌려 `docs/peer-cluster.json` 하나로 합친다. 핵심은 **표준화 기준(평균·표준편차)과 군 중심 좌표를 함께 내보내는 것** — 이게 있어야 브라우저가 우리 회사를 같은 자로 재서 배정할 수 있다.
3. **index.html**: `우리회사_특성()`이 재무데이터에서 4개 특성을 그 자리에서 만들고(매출액 백만원 → 원 환산), `군배정_계산()`이 내보낸 표준화 기준으로 변환해 가장 가까운 군 중심에 배정, `군내순위()`가 순위를 다시 센다. 업종 변경·재무데이터 변경 모두 즉시 반영된다.

### 생성 결과 (DART 실데이터, 총 4,143건 호출)
| 업종 | 매칭 | 분석 | 실루엣 | 군 구성 |
|---|---|---|---|---|
| 제조업 | 1,187곳 | 1,010곳 | 0.643 | 저성장 25 / 안정적 성장 942 / 고성장 43 |
| 정보통신업 | 300곳 | 234곳 | 0.438 | 성장 189 / 고위험 3 / 저성장 42 |
| 서비스업 | 148곳 | 90곳 | 0.618 | 성장 79 / 위기 1 / 혁신 10 |
| 도소매업 | 107곳 | 98곳 | 0.510 | 위험 14 / 성장 가능성 5 / 안정 79 |
| 건설업 | 32곳 | 28곳 | 0.411 | 위험 9 / 안정 17 / 성장 2 |
| 음식·숙박업 | 2곳 | — | — | 상장사 부족으로 제외 |

- 파일이 330KB로 커져 산점도 표시용 점만 **군별 비율 유지 표본 300곳**으로 줄이고 JSON을 압축 저장해 **128KB**로 낮췄다(통계·중위값은 전체 기준 그대로).
- 음식·숙박업처럼 비교군을 만들 수 없는 업종은 영역을 조용히 숨기지 않고 **이유를 안내**한다("코스닥·코넥스 상장사가 거의 없어…").

### 검증
- 업종 6개 전환 시 비교군 회사 수·군 구성·해설이 모두 바뀌는 것 확인(제조업 300점 → 정보통신업 234점 → 건설업 28점 …)
- 재무데이터 변경 반영 확인: 매출 5,100 → 51,000백만원으로 바꾸자 군 내 순위가 **281곳 중 60위 → 14곳 중 1위**로 재계산됨
- `python3 tools/test-e2e.py` (file://) **102개**, 배포 **101개** 전부 통과 — 신규 검사 3건(업종 변경 반영, 데이터 없는 업종 안내, 재무데이터 변경 재계산)

## [2026-09-04] ERP CSV에서 회사명도 인식 · 자동 입력(샘플) 세그먼트 삭제 · 사이드바 로고를 마스코트로 교체 (Task 44)

### 1) CSV 업로드 시 회사명까지 자동 입력
- `api/parse-financials.js`의 Structured Outputs 스키마에 `회사명`(문자열 또는 null)을 추가하고, 프롬프트에 "CSV 위쪽 제목·머리말의 상호만 담고, 파일명·보고서 종류·기간은 회사명이 아니다"를 명시
- `ERP_CSV_불러오기()`가 읽은 회사명을 기업정보에 채운다. **이미 입력해 둔 회사명이 있으면 덮어쓰지 않는다**(사용자가 직접 적은 값 보호). 상태 문구도 "회사명(OO)과 매출액·… 채웠습니다"로 표시
- 배포 검증: `대한정밀공업(주) 손익계산서` 머리말이 있는 CSV 업로드 → 회사명 `대한정밀공업(주)` + 재무 6항목 모두 자동 입력 확인

### 2) 재무데이터 "자동 입력(샘플)" 세그먼트 삭제
- `#fin-mode-switch`(직접 입력 / 자동 입력(샘플)) 전체와 `재무입력모드_표시()`·`바인딩_재무입력모드()`를 제거. 이제 재무데이터를 채우는 경로는 **직접 입력**과 **ERP CSV 불러오기** 두 가지다
- `샘플불러오기()` 함수 자체는 남겼다 — 화면 진입점은 없지만 자동 검증(E2E)에서 데모 데이터를 채우는 데 계속 쓰인다. 테스트는 `page.evaluate('샘플불러오기()')`로 호출하도록 바꿨다

### 3) 사이드바 로고를 마스코트로 교체
사용자가 제공한 이미지(길안내 핀 캐릭터가 실적 그래프를 든 모습)를 **인라인 SVG로 직접 그려** 기존 나침반 심볼을 대체했다(외부 이미지 파일 없이 단일 HTML 유지). 색은 STEERING 토큰만 사용 — 오렌지 `#FF6000`, 외곽선 `#333D4B`, 얼굴 `#FFF6EF`, 그래프 막대 `#D1D6DC`/`#98A2B3`/오렌지. 배경 사각형을 없애고 마크 크기를 40 → 46px로 키웠으며, 팔이 박스 밖으로 나오도록 `overflow: visible` 적용. 브랜드명 "택스네비"와 소개 문구는 그대로 두었다(제품명이 사라지면 화면 식별이 어려워짐). 태블릿 아이콘 모드(72px 사이드바)에서도 넘치지 않는 것 확인

### 검증
- `python3 tools/test-e2e.py` (file://) **101개**, 배포 **101개** 전부 통과 — 신규 검사 2건(세그먼트 삭제 확인, CSV 회사명 인식), 샘플 로딩 방식 변경 반영
- 작업 중 내가 넣은 검사가 회사명을 비운 뒤 되돌리지 않아 뒤쪽 검사 2건이 깨졌고(`새로고침: 회사명 유지`, `인쇄: 머리글`), 복구 코드를 `if/else` 사이에 잘못 넣어 구문 오류까지 냈다 — 둘 다 바로잡음

## [2026-09-04] K-means 산점도 축 제목 추가 + "우리 회사" 라벨 겹침 수정 (Task 45)

사용자 요청: "세로는 3년 매출 성장률이라고 표시해주고 가로에는 매출 규모라고 표시" → 이후 화면 확인 후 "수정해".
- **축 제목 추가**: 가로 "매출 규모 (log10, 오른쪽일수록 큼)", 세로 "3년 매출 성장률 (연평균)"(90도 회전). 세로 제목 자리를 위해 왼쪽 여백을 46 → 62px로 넓혔다
- **라벨 겹침 수정**: 화면을 보니 "우리 회사" 글자 뒤에 다른 회사 점이 깔려 "우리●회사"처럼 읽혔다. DOM 좌표로 확인해 실제로 `(주)수젠텍` 점이 라벨 영역 안에 있는 것을 확인
  - 1차로 흰 테두리(halo)를 둘렀지만 점이 여전히 글자 사이로 비쳐 근본 해결이 아니었다
  - 2차로 **라벨 자동 배치**를 구현: 마커 주변 16개 후보 위치(위·아래·좌·우·대각)를 돌며 다른 회사 점이 가장 적게 걸리는 자리를 고르고, 마커에서 떨어지면 가는 연결선으로 이어 준다. 그래프 밖으로 나가는 후보는 제외
  - 5개 업종(제조·정보통신·도소매·건설·서비스) 전부 겹침 0개 확인. 후보 6곳일 때는 도소매업에서 2개가 남아 16곳으로 늘렸다

### 검증
- `python3 tools/test-e2e.py` (file://) **103개**, 배포 **103개** 전부 통과 — 신규 검사 2건(축 제목 표시, 라벨-점 겹침 0개를 `getBBox()` 좌표로 실측)

## [2026-09-04] 재무데이터 7개 항목 추가 + 부가세·법인세 계산 연동 (Task 46)

사용자 요청: "매출원가, 판관비, 당기순이익, 법인세비용, 무형자산, 이연법인세자산·부채, 법인세납부액을 재무데이터 항목에 추가하고 부가세·법인세가 계산되게, 모든 항목에서 연동되게".

### 추가 항목 (모두 선택 입력 — 재무항목 7개 → 14개, 입력칸 21 → 42개)
매출원가 · 판매관리비 · 당기순이익(음수 허용) · 법인세비용 · 무형자산 · 이연법인세자산·부채(자산 +, 부채 −) · 법인세 납부액. 필수 항목은 그대로 5개라 입력 진행률(18개)은 변하지 않는다.

### 계산 연동
1. **부가세** — `과세매입_추정()` 신설. 과세매입액을 직접 넣으면 그대로 쓰고, 비어 있으면 **매출원가 + 판관비 − 인건비 − 감가상각비**로 추정한다(인건비·감가상각비는 매입세액 공제 대상이 아니라 제외). 추정을 쓴 경우 부가세 방식 문구에 "매입액은 매출원가·판관비로 추정"을 붙인다. 검증: 과세매입액을 비우면 (5,100−3,170)×10% = **1.93억원**으로 계산됨
2. **법인세 과세표준** — `과세표준_추정()` 신설. 당기순이익+법인세비용이 있으면 **법인세차감전순이익**을 쓰고(가장 정확), 없으면 기존처럼 영업이익−이자비용으로 근사한다. 샘플 기준 과세표준이 360백만원으로 잡히고 표에 근거 방식이 함께 표시된다
3. **예측·시뮬레이션 연동** — 실적의 영업외손익(세전이익 − (영업이익−이자비용))을 예측·시뮬레이션에도 같은 크기로 반영해, 슬라이더를 움직여도 과세표준 기준이 일관되게 유지된다
4. **통합투자세액공제** — 과거 투자액 추정에 **무형자산 증가분**을 더한다. 샘플 기준 과거 평균이 387.5 → 402.5백만원으로 올라 추가공제가 125만원 → 0원이 되고 공제액은 41,250,000 → **40,000,000원**(무형자산까지 반영한 정상 결과)
5. **결손금 소급공제 경고** — **법인세 납부액**을 넣으면 "직전 사업연도 법인세 납부액 ○○이 환급 한도입니다"로 한도를 금액으로 알려 준다(없으면 입력 안내)

### 입력 모순 감지 (작업 중 발견)
영업이익을 적자로 고쳐도 당기순이익·법인세비용이 예전 값으로 남아 있으면 과세표준이 흑자로 계산되는 문제가 있었다(자동 검증의 "결손: 법인세 0" 항목이 이를 잡아냈다). **흑자/적자 방향이 서로 엇갈릴 때는 필수 입력인 영업이익 쪽을 따르고, 분석결과 상단에 경고**를 띄우도록 했다.

### 검증
- 손계산 대조: 과세표준 360 → 법인세 52,000,000원·지방 5,200,000원·합계 5,720만원, 투자공제 40,000,000원, 부가세 추정 1.93억원 모두 일치
- `python3 tools/test-e2e.py` (file://) **105개**, 배포 **105개** 전부 통과 — 신규 검사 2건(신규 7항목 채워짐, 과세표준이 세전이익 기준), 기존 기대값 2건 갱신(입력칸 42개, 투자공제 40,000,000원)

## [2026-09-04] K-means 산점도 A군 색을 주황 → 연두로 변경

사용자 요청: "a군은 주황색 말고 연두색으로, 업종이 바뀌어도 똑같이 적용되게".
- `--cluster-a`를 오렌지(`--chart-forecast`) → **연두 `#8BC34A`**로 변경. 임의로 고르지 않고 dataviz 검증 스크립트(`validate_palette.js --pairs all`)로 후보 5개를 검사해, 밝기 대역·색맹 판별·정상시각 기준을 모두 통과하는 것 중 가장 분리가 좋은 색을 선택(#9CCC65·#A0CE4E는 너무 밝아 밝기 대역 탈락, #66BB6A는 하늘색과 tritan 분리가 4.8로 약함)
- 군 색은 원래부터 GPT 라벨(A/B/C) 기준 매핑이라 업종과 무관하게 동일하다 — 5개 업종(제조·정보통신·도소매·건설·서비스) 모두 A=연두·B=하늘·C=회색으로 나오는 것을 실제 렌더링 값(`getComputedStyle().fill`)으로 확인
- 부수 효과: A군에서 주황이 빠지면서 **"우리 회사" 주황 다이아몬드 마커가 세 군 어느 색과도 겹치지 않아 더 잘 보인다**
- `python3 tools/test-e2e.py` **106개**, 배포 106개 전부 통과 — 신규 검사 1건(세 군 색이 지정한 RGB로 고정되는지 실측)

## [2026-09-04] 실시간 시뮬레이션 세 슬라이더 시작 위치 통일

사용자 요청: "매출 성장률, 영업이익률, 이자비용 비율 모두 동일한 지점에서 스크롤 할 수 있게".
기존에는 손잡이 시작 위치가 제각각이었다 — 매출 성장률 50%(중앙), 영업이익률 47%, 이자비용 비율 2%(거의 맨 왼쪽). 뒤 두 개는 절대 비율이라 기본값(기준연도 실적)이 눈금 어디에 놓일지 데이터에 따라 달라졌기 때문이다.
- **세 항목 모두 "기준연도 실적 대비 증감(%p)"으로 통일**하고 범위를 −30~30으로 맞춰 **전부 0(정중앙)에서 시작**하게 했다. 0이면 예측이 실적과 같아지는 성질은 그대로 유지
- 절대 비율을 못 보게 되는 단점은 라벨로 보완했다 — "영업이익률 증감 (기준 7.5% → **적용 12.5%**)"처럼 슬라이더를 움직이면 실제 적용되는 비율이 실시간으로 표시되고, 예측 표 부제에도 적용 절대값이 들어간다
- 이자비용 비율은 음수가 될 수 없어 `Math.max(0, 기준 + 증감)`으로 하한을 둔다
- 검증: 세 슬라이더 손잡이 위치가 모두 50%, 0일 때 예측 = 실적(영업이익 3.8억원·과세표준 3.6억원) 확인
- `python3 tools/test-e2e.py` **108개**, 배포 108개 전부 통과 — 신규 검사 2건(세 슬라이더 위치 [50,50,50], 라벨의 기준→적용 표시)
- 작업 중 새 검사를 기존 검사 사이에 끼워 넣어 성장률을 0으로 되돌리는 바람에 뒤의 "표 갱신 56.1억원" 검사가 깨졌다 — 원상 복구 단계 뒤로 옮겨 해결

## [2026-09-04] 세무 정합성 수정 — 최저한세 · 혜택 성격 분리 · 중복지원 경고 (Task 47)

코드 전체 세무 검토에서 나온 지적 중 우선순위 1~3번을 수정했다.

### 1) 최저한세(조세특례제한법 §132) 미적용 → 적용
기존에는 `반영후법인세 = max(0, 법인세 − 총절감)`이라 공제만 충분하면 법인세가 **0원까지 떨어졌다**. 실제로는 중소기업 최저한세율 7%가 적용돼 그 아래로 낼 수 없다.
- `docs/tax-rules.json`에 `최저한세`(중소기업 7%, 일반기업 10~17%, 배제분 10년 이월) 추가
- `최저한세 = 감면 전 과세표준 × 7%`, `반영후 법인세 = max(법인세 − 세액공제, 최저한세)`로 변경
- 샘플 기준 변화: 과세표준 360백만 → 최저한세 **2,520만원**. 세액공제 5,822만원 중 실제 적용은 **2,680만원**뿐이고 **3,142만원은 이월** 대상. 화면 표시도 "5,720만원 → 520만원"에서 **"5,720만원 → 3,040만원"**으로 바로잡혔고, 이월 금액을 문구로 안내한다

### 2) 성격이 다른 혜택의 단순 합산 → 유형별 분리
세액공제·현금지원금·부가세공제·소득공제를 모두 더해 "법인세에서 차감"하고 있었다(특히 **고용지원금을 법인세에서 빼는 것은 명백한 오류**).
- 규칙 파일의 각 제도에 `혜택유형`(법인세공제/부가세공제/현금지원/소득공제)을 부여
- 계산 결과를 `법인세공제액`·`부가세공제액`·`현금지원액`·`소득공제절감`으로 나누고, **법인세에서는 법인세공제액만 차감**한다. 부가세공제는 `반영후_부가세`로 따로 계산
- 요약 KPI를 "절감 예상액" → "예상 혜택 합계"로 바꾸고, 그 아래에 **어느 세금에 어떻게 반영되는지 항목별 안내**를 붙였다("현금 지원금 720만원 — 세금 감면이 아니라 기관에서 받는 돈(과세 대상 수입일 수 있음)")
- 전략 표의 각 행에도 혜택 성격을 표기

### 3) 중복지원 배제 안내 추가
통합고용세액공제와 정부 고용지원금(두루누리·청년일자리도약장려금)이 **동시에 적용 가능으로 나올 때** 조특법 §127(중복지원 배제)·고용노동부 지침을 근거로 경고 배너를 띄운다. 단정하지 않고 "관할 세무서·고용센터에 중복 가능 여부를 먼저 확인하라"로 안내

### 검증
- 손계산 대조 전부 일치: 산출세액 52,000,000 / 세액공제 58,218,079 / 최저한세 25,200,000 / 실제 공제 26,800,000 / 이월 31,418,079 / 반영 후 세부담 30,400,000(법인세 25,200,000 + 지방세 5,200,000)
- `python3 tools/test-e2e.py` (file://) **114개**, 배포 **114개** 전부 통과 — 세무 검증 6건 신규 추가(최저한세 금액, 최저한세 하한 준수, 이월 분리, 현금지원 미혼입, 성격 분리 안내, 중복지원 경고)

### 남은 지적 사항(미수정, 사용자 판단 대기)
중소기업 요건 미확인 / 부가세 면세·영세율·불공제 매입세액 미반영 / 매입액 추정의 한계 / 시뮬레이션 감가상각비 고정 / 지방소득세 근거 표현 / 근로소득증대·투자공제의 "직전 3년 평균"을 2년치로 대용 / 상시근로자 수를 월평균이 아닌 기말 인원으로 입력
